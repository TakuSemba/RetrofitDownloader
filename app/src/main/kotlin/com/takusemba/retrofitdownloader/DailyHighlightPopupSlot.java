// Code generated by Wire protocol buffer compiler, do not edit.
// Source file: api/highlight_popup.proto at 20:1
package tv.abema.protos;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.WireField;
import com.squareup.wire.internal.Internal;
import java.io.IOException;
import java.lang.Object;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import okio.ByteString;

/**
 * / 今日のみどころポップアップ放送枠
 */
public final class DailyHighlightPopupSlot extends Message<DailyHighlightPopupSlot, DailyHighlightPopupSlot.Builder> {
  public static final ProtoAdapter<DailyHighlightPopupSlot> ADAPTER = new ProtoAdapter_DailyHighlightPopupSlot();

  private static final long serialVersionUID = 0L;

  public static final String DEFAULT_SLOTID = "";

  public static final String DEFAULT_TITLE = "";

  public static final String DEFAULT_THUMBIMG = "";

  /**
   * / 放送枠ID
   */
  @WireField(
      tag = 1,
      adapter = "com.squareup.wire.ProtoAdapter#STRING"
  )
  public final String slotId;

  /**
   * / タイトル
   */
  @WireField(
      tag = 2,
      adapter = "com.squareup.wire.ProtoAdapter#STRING"
  )
  public final String title;

  /**
   * / 画像名(代表番組の番組サムネ、シーンサムネのうち1つ/thumb01-06)
   */
  @WireField(
      tag = 3,
      adapter = "com.squareup.wire.ProtoAdapter#STRING"
  )
  public final String thumbImg;

  public DailyHighlightPopupSlot(String slotId, String title, String thumbImg) {
    this(slotId, title, thumbImg, ByteString.EMPTY);
  }

  public DailyHighlightPopupSlot(String slotId, String title, String thumbImg, ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    this.slotId = slotId;
    this.title = title;
    this.thumbImg = thumbImg;
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.slotId = slotId;
    builder.title = title;
    builder.thumbImg = thumbImg;
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public boolean equals(Object other) {
    if (other == this) return true;
    if (!(other instanceof DailyHighlightPopupSlot)) return false;
    DailyHighlightPopupSlot o = (DailyHighlightPopupSlot) other;
    return Internal.equals(unknownFields(), o.unknownFields())
        && Internal.equals(slotId, o.slotId)
        && Internal.equals(title, o.title)
        && Internal.equals(thumbImg, o.thumbImg);
  }

  @Override
  public int hashCode() {
    int result = super.hashCode;
    if (result == 0) {
      result = unknownFields().hashCode();
      result = result * 37 + (slotId != null ? slotId.hashCode() : 0);
      result = result * 37 + (title != null ? title.hashCode() : 0);
      result = result * 37 + (thumbImg != null ? thumbImg.hashCode() : 0);
      super.hashCode = result;
    }
    return result;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    if (slotId != null) builder.append(", slotId=").append(slotId);
    if (title != null) builder.append(", title=").append(title);
    if (thumbImg != null) builder.append(", thumbImg=").append(thumbImg);
    return builder.replace(0, 2, "DailyHighlightPopupSlot{").append('}').toString();
  }

  public static final class Builder extends Message.Builder<DailyHighlightPopupSlot, Builder> {
    public String slotId;

    public String title;

    public String thumbImg;

    public Builder() {
    }

    /**
     * / 放送枠ID
     */
    public Builder slotId(String slotId) {
      this.slotId = slotId;
      return this;
    }

    /**
     * / タイトル
     */
    public Builder title(String title) {
      this.title = title;
      return this;
    }

    /**
     * / 画像名(代表番組の番組サムネ、シーンサムネのうち1つ/thumb01-06)
     */
    public Builder thumbImg(String thumbImg) {
      this.thumbImg = thumbImg;
      return this;
    }

    @Override
    public DailyHighlightPopupSlot build() {
      return new DailyHighlightPopupSlot(slotId, title, thumbImg, buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_DailyHighlightPopupSlot extends ProtoAdapter<DailyHighlightPopupSlot> {
    ProtoAdapter_DailyHighlightPopupSlot() {
      super(FieldEncoding.LENGTH_DELIMITED, DailyHighlightPopupSlot.class);
    }

    @Override
    public int encodedSize(DailyHighlightPopupSlot value) {
      return (value.slotId != null ? ProtoAdapter.STRING.encodedSizeWithTag(1, value.slotId) : 0)
          + (value.title != null ? ProtoAdapter.STRING.encodedSizeWithTag(2, value.title) : 0)
          + (value.thumbImg != null ? ProtoAdapter.STRING.encodedSizeWithTag(3, value.thumbImg) : 0)
          + value.unknownFields().size();
    }

    @Override
    public void encode(ProtoWriter writer, DailyHighlightPopupSlot value) throws IOException {
      if (value.slotId != null) ProtoAdapter.STRING.encodeWithTag(writer, 1, value.slotId);
      if (value.title != null) ProtoAdapter.STRING.encodeWithTag(writer, 2, value.title);
      if (value.thumbImg != null) ProtoAdapter.STRING.encodeWithTag(writer, 3, value.thumbImg);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public DailyHighlightPopupSlot decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.slotId(ProtoAdapter.STRING.decode(reader)); break;
          case 2: builder.title(ProtoAdapter.STRING.decode(reader)); break;
          case 3: builder.thumbImg(ProtoAdapter.STRING.decode(reader)); break;
          default: {
            FieldEncoding fieldEncoding = reader.peekFieldEncoding();
            Object value = fieldEncoding.rawProtoAdapter().decode(reader);
            builder.addUnknownField(tag, fieldEncoding, value);
          }
        }
      }
      reader.endMessage(token);
      return builder.build();
    }

    @Override
    public DailyHighlightPopupSlot redact(DailyHighlightPopupSlot value) {
      Builder builder = value.newBuilder();
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
