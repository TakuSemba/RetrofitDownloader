// Code generated by Wire protocol buffer compiler, do not edit.
// Source file: api/video.proto at 61:1
package tv.abema.protos;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.WireField;
import com.squareup.wire.internal.Internal;
import java.io.IOException;
import java.lang.Boolean;
import java.lang.Object;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import okio.ByteString;

/**
 * / ビデオ番組メディアステータス
 */
public final class VideoProgramMediaStatus extends Message<VideoProgramMediaStatus, VideoProgramMediaStatus.Builder> {
  public static final ProtoAdapter<VideoProgramMediaStatus> ADAPTER = new ProtoAdapter_VideoProgramMediaStatus();

  private static final long serialVersionUID = 0L;

  public static final Boolean DEFAULT_DRM = false;

  /**
   * / DRM配信
   */
  @WireField(
      tag = 1,
      adapter = "com.squareup.wire.ProtoAdapter#BOOL"
  )
  public final Boolean drm;

  public VideoProgramMediaStatus(Boolean drm) {
    this(drm, ByteString.EMPTY);
  }

  public VideoProgramMediaStatus(Boolean drm, ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    this.drm = drm;
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.drm = drm;
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public boolean equals(Object other) {
    if (other == this) return true;
    if (!(other instanceof VideoProgramMediaStatus)) return false;
    VideoProgramMediaStatus o = (VideoProgramMediaStatus) other;
    return Internal.equals(unknownFields(), o.unknownFields())
        && Internal.equals(drm, o.drm);
  }

  @Override
  public int hashCode() {
    int result = super.hashCode;
    if (result == 0) {
      result = unknownFields().hashCode();
      result = result * 37 + (drm != null ? drm.hashCode() : 0);
      super.hashCode = result;
    }
    return result;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    if (drm != null) builder.append(", drm=").append(drm);
    return builder.replace(0, 2, "VideoProgramMediaStatus{").append('}').toString();
  }

  public static final class Builder extends Message.Builder<VideoProgramMediaStatus, Builder> {
    public Boolean drm;

    public Builder() {
    }

    /**
     * / DRM配信
     */
    public Builder drm(Boolean drm) {
      this.drm = drm;
      return this;
    }

    @Override
    public VideoProgramMediaStatus build() {
      return new VideoProgramMediaStatus(drm, buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_VideoProgramMediaStatus extends ProtoAdapter<VideoProgramMediaStatus> {
    ProtoAdapter_VideoProgramMediaStatus() {
      super(FieldEncoding.LENGTH_DELIMITED, VideoProgramMediaStatus.class);
    }

    @Override
    public int encodedSize(VideoProgramMediaStatus value) {
      return (value.drm != null ? ProtoAdapter.BOOL.encodedSizeWithTag(1, value.drm) : 0)
          + value.unknownFields().size();
    }

    @Override
    public void encode(ProtoWriter writer, VideoProgramMediaStatus value) throws IOException {
      if (value.drm != null) ProtoAdapter.BOOL.encodeWithTag(writer, 1, value.drm);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public VideoProgramMediaStatus decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.drm(ProtoAdapter.BOOL.decode(reader)); break;
          default: {
            FieldEncoding fieldEncoding = reader.peekFieldEncoding();
            Object value = fieldEncoding.rawProtoAdapter().decode(reader);
            builder.addUnknownField(tag, fieldEncoding, value);
          }
        }
      }
      reader.endMessage(token);
      return builder.build();
    }

    @Override
    public VideoProgramMediaStatus redact(VideoProgramMediaStatus value) {
      Builder builder = value.newBuilder();
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
