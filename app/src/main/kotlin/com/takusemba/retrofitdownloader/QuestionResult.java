// Code generated by Wire protocol buffer compiler, do not edit.
// Source file: api/question.proto at 37:1
package tv.abema.protos;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.WireField;
import com.squareup.wire.internal.Internal;
import java.io.IOException;
import java.lang.Object;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import java.util.List;
import okio.ByteString;

/**
 * / 質問の結果
 */
public final class QuestionResult extends Message<QuestionResult, QuestionResult.Builder> {
  public static final ProtoAdapter<QuestionResult> ADAPTER = new ProtoAdapter_QuestionResult();

  private static final long serialVersionUID = 0L;

  /**
   * / 各選択肢の結果
   */
  @WireField(
      tag = 1,
      adapter = "tv.abema.protos.ResultChoice#ADAPTER",
      label = WireField.Label.REPEATED
  )
  public final List<ResultChoice> choices;

  public QuestionResult(List<ResultChoice> choices) {
    this(choices, ByteString.EMPTY);
  }

  public QuestionResult(List<ResultChoice> choices, ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    this.choices = Internal.immutableCopyOf("choices", choices);
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.choices = Internal.copyOf("choices", choices);
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public boolean equals(Object other) {
    if (other == this) return true;
    if (!(other instanceof QuestionResult)) return false;
    QuestionResult o = (QuestionResult) other;
    return Internal.equals(unknownFields(), o.unknownFields())
        && Internal.equals(choices, o.choices);
  }

  @Override
  public int hashCode() {
    int result = super.hashCode;
    if (result == 0) {
      result = unknownFields().hashCode();
      result = result * 37 + (choices != null ? choices.hashCode() : 1);
      super.hashCode = result;
    }
    return result;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    if (choices != null) builder.append(", choices=").append(choices);
    return builder.replace(0, 2, "QuestionResult{").append('}').toString();
  }

  public static final class Builder extends Message.Builder<QuestionResult, Builder> {
    public List<ResultChoice> choices;

    public Builder() {
      choices = Internal.newMutableList();
    }

    /**
     * / 各選択肢の結果
     */
    public Builder choices(List<ResultChoice> choices) {
      Internal.checkElementsNotNull(choices);
      this.choices = choices;
      return this;
    }

    @Override
    public QuestionResult build() {
      return new QuestionResult(choices, buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_QuestionResult extends ProtoAdapter<QuestionResult> {
    ProtoAdapter_QuestionResult() {
      super(FieldEncoding.LENGTH_DELIMITED, QuestionResult.class);
    }

    @Override
    public int encodedSize(QuestionResult value) {
      return ResultChoice.ADAPTER.asRepeated().encodedSizeWithTag(1, value.choices)
          + value.unknownFields().size();
    }

    @Override
    public void encode(ProtoWriter writer, QuestionResult value) throws IOException {
      if (value.choices != null) ResultChoice.ADAPTER.asRepeated().encodeWithTag(writer, 1, value.choices);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public QuestionResult decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.choices.add(ResultChoice.ADAPTER.decode(reader)); break;
          default: {
            FieldEncoding fieldEncoding = reader.peekFieldEncoding();
            Object value = fieldEncoding.rawProtoAdapter().decode(reader);
            builder.addUnknownField(tag, fieldEncoding, value);
          }
        }
      }
      reader.endMessage(token);
      return builder.build();
    }

    @Override
    public QuestionResult redact(QuestionResult value) {
      Builder builder = value.newBuilder();
      Internal.redactElements(builder.choices, ResultChoice.ADAPTER);
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
