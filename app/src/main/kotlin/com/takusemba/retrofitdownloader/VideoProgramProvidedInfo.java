// Code generated by Wire protocol buffer compiler, do not edit.
// Source file: api/video.proto at 40:1
package tv.abema.protos;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.WireField;
import com.squareup.wire.internal.Internal;
import java.io.IOException;
import java.lang.Object;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import java.util.List;
import okio.ByteString;

/**
 * / ビデオ番組納品情報
 */
public final class VideoProgramProvidedInfo extends Message<VideoProgramProvidedInfo, VideoProgramProvidedInfo.Builder> {
  public static final ProtoAdapter<VideoProgramProvidedInfo> ADAPTER = new ProtoAdapter_VideoProgramProvidedInfo();

  private static final long serialVersionUID = 0L;

  public static final String DEFAULT_THUMBIMG = "";

  /**
   * / サムネイル
   */
  @WireField(
      tag = 1,
      adapter = "com.squareup.wire.ProtoAdapter#STRING"
  )
  public final String thumbImg;

  /**
   * / シーンサムネイル一覧
   */
  @WireField(
      tag = 2,
      adapter = "com.squareup.wire.ProtoAdapter#STRING",
      label = WireField.Label.REPEATED
  )
  public final List<String> sceneThumbImgs;

  public VideoProgramProvidedInfo(String thumbImg, List<String> sceneThumbImgs) {
    this(thumbImg, sceneThumbImgs, ByteString.EMPTY);
  }

  public VideoProgramProvidedInfo(String thumbImg, List<String> sceneThumbImgs, ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    this.thumbImg = thumbImg;
    this.sceneThumbImgs = Internal.immutableCopyOf("sceneThumbImgs", sceneThumbImgs);
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.thumbImg = thumbImg;
    builder.sceneThumbImgs = Internal.copyOf("sceneThumbImgs", sceneThumbImgs);
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public boolean equals(Object other) {
    if (other == this) return true;
    if (!(other instanceof VideoProgramProvidedInfo)) return false;
    VideoProgramProvidedInfo o = (VideoProgramProvidedInfo) other;
    return Internal.equals(unknownFields(), o.unknownFields())
        && Internal.equals(thumbImg, o.thumbImg)
        && Internal.equals(sceneThumbImgs, o.sceneThumbImgs);
  }

  @Override
  public int hashCode() {
    int result = super.hashCode;
    if (result == 0) {
      result = unknownFields().hashCode();
      result = result * 37 + (thumbImg != null ? thumbImg.hashCode() : 0);
      result = result * 37 + (sceneThumbImgs != null ? sceneThumbImgs.hashCode() : 1);
      super.hashCode = result;
    }
    return result;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    if (thumbImg != null) builder.append(", thumbImg=").append(thumbImg);
    if (sceneThumbImgs != null) builder.append(", sceneThumbImgs=").append(sceneThumbImgs);
    return builder.replace(0, 2, "VideoProgramProvidedInfo{").append('}').toString();
  }

  public static final class Builder extends Message.Builder<VideoProgramProvidedInfo, Builder> {
    public String thumbImg;

    public List<String> sceneThumbImgs;

    public Builder() {
      sceneThumbImgs = Internal.newMutableList();
    }

    /**
     * / サムネイル
     */
    public Builder thumbImg(String thumbImg) {
      this.thumbImg = thumbImg;
      return this;
    }

    /**
     * / シーンサムネイル一覧
     */
    public Builder sceneThumbImgs(List<String> sceneThumbImgs) {
      Internal.checkElementsNotNull(sceneThumbImgs);
      this.sceneThumbImgs = sceneThumbImgs;
      return this;
    }

    @Override
    public VideoProgramProvidedInfo build() {
      return new VideoProgramProvidedInfo(thumbImg, sceneThumbImgs, buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_VideoProgramProvidedInfo extends ProtoAdapter<VideoProgramProvidedInfo> {
    ProtoAdapter_VideoProgramProvidedInfo() {
      super(FieldEncoding.LENGTH_DELIMITED, VideoProgramProvidedInfo.class);
    }

    @Override
    public int encodedSize(VideoProgramProvidedInfo value) {
      return (value.thumbImg != null ? ProtoAdapter.STRING.encodedSizeWithTag(1, value.thumbImg) : 0)
          + ProtoAdapter.STRING.asRepeated().encodedSizeWithTag(2, value.sceneThumbImgs)
          + value.unknownFields().size();
    }

    @Override
    public void encode(ProtoWriter writer, VideoProgramProvidedInfo value) throws IOException {
      if (value.thumbImg != null) ProtoAdapter.STRING.encodeWithTag(writer, 1, value.thumbImg);
      if (value.sceneThumbImgs != null) ProtoAdapter.STRING.asRepeated().encodeWithTag(writer, 2, value.sceneThumbImgs);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public VideoProgramProvidedInfo decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.thumbImg(ProtoAdapter.STRING.decode(reader)); break;
          case 2: builder.sceneThumbImgs.add(ProtoAdapter.STRING.decode(reader)); break;
          default: {
            FieldEncoding fieldEncoding = reader.peekFieldEncoding();
            Object value = fieldEncoding.rawProtoAdapter().decode(reader);
            builder.addUnknownField(tag, fieldEncoding, value);
          }
        }
      }
      reader.endMessage(token);
      return builder.build();
    }

    @Override
    public VideoProgramProvidedInfo redact(VideoProgramProvidedInfo value) {
      Builder builder = value.newBuilder();
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
