// Code generated by Wire protocol buffer compiler, do not edit.
// Source file: api/user.proto at 112:1
package tv.abema.protos;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.WireField;
import com.squareup.wire.internal.Internal;
import java.io.IOException;
import java.lang.Object;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import okio.ByteString;

/**
 * Represents a response of register user
 */
public final class RegisterUserResponse extends Message<RegisterUserResponse, RegisterUserResponse.Builder> {
  public static final ProtoAdapter<RegisterUserResponse> ADAPTER = new ProtoAdapter_RegisterUserResponse();

  private static final long serialVersionUID = 0L;

  public static final String DEFAULT_TOKEN = "";

  /**
   * / Registered user profile
   */
  @WireField(
      tag = 1,
      adapter = "tv.abema.protos.Profile#ADAPTER"
  )
  public final Profile profile;

  /**
   * / Request token
   */
  @WireField(
      tag = 2,
      adapter = "com.squareup.wire.ProtoAdapter#STRING"
  )
  public final String token;

  public RegisterUserResponse(Profile profile, String token) {
    this(profile, token, ByteString.EMPTY);
  }

  public RegisterUserResponse(Profile profile, String token, ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    this.profile = profile;
    this.token = token;
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.profile = profile;
    builder.token = token;
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public boolean equals(Object other) {
    if (other == this) return true;
    if (!(other instanceof RegisterUserResponse)) return false;
    RegisterUserResponse o = (RegisterUserResponse) other;
    return Internal.equals(unknownFields(), o.unknownFields())
        && Internal.equals(profile, o.profile)
        && Internal.equals(token, o.token);
  }

  @Override
  public int hashCode() {
    int result = super.hashCode;
    if (result == 0) {
      result = unknownFields().hashCode();
      result = result * 37 + (profile != null ? profile.hashCode() : 0);
      result = result * 37 + (token != null ? token.hashCode() : 0);
      super.hashCode = result;
    }
    return result;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    if (profile != null) builder.append(", profile=").append(profile);
    if (token != null) builder.append(", token=").append(token);
    return builder.replace(0, 2, "RegisterUserResponse{").append('}').toString();
  }

  public static final class Builder extends Message.Builder<RegisterUserResponse, Builder> {
    public Profile profile;

    public String token;

    public Builder() {
    }

    /**
     * / Registered user profile
     */
    public Builder profile(Profile profile) {
      this.profile = profile;
      return this;
    }

    /**
     * / Request token
     */
    public Builder token(String token) {
      this.token = token;
      return this;
    }

    @Override
    public RegisterUserResponse build() {
      return new RegisterUserResponse(profile, token, buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_RegisterUserResponse extends ProtoAdapter<RegisterUserResponse> {
    ProtoAdapter_RegisterUserResponse() {
      super(FieldEncoding.LENGTH_DELIMITED, RegisterUserResponse.class);
    }

    @Override
    public int encodedSize(RegisterUserResponse value) {
      return (value.profile != null ? Profile.ADAPTER.encodedSizeWithTag(1, value.profile) : 0)
          + (value.token != null ? ProtoAdapter.STRING.encodedSizeWithTag(2, value.token) : 0)
          + value.unknownFields().size();
    }

    @Override
    public void encode(ProtoWriter writer, RegisterUserResponse value) throws IOException {
      if (value.profile != null) Profile.ADAPTER.encodeWithTag(writer, 1, value.profile);
      if (value.token != null) ProtoAdapter.STRING.encodeWithTag(writer, 2, value.token);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public RegisterUserResponse decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.profile(Profile.ADAPTER.decode(reader)); break;
          case 2: builder.token(ProtoAdapter.STRING.decode(reader)); break;
          default: {
            FieldEncoding fieldEncoding = reader.peekFieldEncoding();
            Object value = fieldEncoding.rawProtoAdapter().decode(reader);
            builder.addUnknownField(tag, fieldEncoding, value);
          }
        }
      }
      reader.endMessage(token);
      return builder.build();
    }

    @Override
    public RegisterUserResponse redact(RegisterUserResponse value) {
      Builder builder = value.newBuilder();
      if (builder.profile != null) builder.profile = Profile.ADAPTER.redact(builder.profile);
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
