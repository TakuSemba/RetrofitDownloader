// Code generated by Wire protocol buffer compiler, do not edit.
// Source file: api/channel.proto at 11:1
package tv.abema.protos;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.WireField;
import com.squareup.wire.internal.Internal;
import java.io.IOException;
import java.lang.Object;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import okio.ByteString;

/**
 * / 放送中のチャネル情報
 */
public final class BroadcastChannel extends Message<BroadcastChannel, BroadcastChannel.Builder> {
  public static final ProtoAdapter<BroadcastChannel> ADAPTER = new ProtoAdapter_BroadcastChannel();

  private static final long serialVersionUID = 0L;

  public static final String DEFAULT_ID = "";

  public static final String DEFAULT_NAME = "";

  /**
   * / チャネルID
   */
  @WireField(
      tag = 1,
      adapter = "com.squareup.wire.ProtoAdapter#STRING"
  )
  public final String id;

  /**
   * / チャネル名
   */
  @WireField(
      tag = 2,
      adapter = "com.squareup.wire.ProtoAdapter#STRING"
  )
  public final String name;

  /**
   * / 再生情報
   */
  @WireField(
      tag = 3,
      adapter = "tv.abema.protos.ChannelPlayback#ADAPTER"
  )
  public final ChannelPlayback playback;

  public BroadcastChannel(String id, String name, ChannelPlayback playback) {
    this(id, name, playback, ByteString.EMPTY);
  }

  public BroadcastChannel(String id, String name, ChannelPlayback playback, ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    this.id = id;
    this.name = name;
    this.playback = playback;
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.id = id;
    builder.name = name;
    builder.playback = playback;
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public boolean equals(Object other) {
    if (other == this) return true;
    if (!(other instanceof BroadcastChannel)) return false;
    BroadcastChannel o = (BroadcastChannel) other;
    return Internal.equals(unknownFields(), o.unknownFields())
        && Internal.equals(id, o.id)
        && Internal.equals(name, o.name)
        && Internal.equals(playback, o.playback);
  }

  @Override
  public int hashCode() {
    int result = super.hashCode;
    if (result == 0) {
      result = unknownFields().hashCode();
      result = result * 37 + (id != null ? id.hashCode() : 0);
      result = result * 37 + (name != null ? name.hashCode() : 0);
      result = result * 37 + (playback != null ? playback.hashCode() : 0);
      super.hashCode = result;
    }
    return result;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    if (id != null) builder.append(", id=").append(id);
    if (name != null) builder.append(", name=").append(name);
    if (playback != null) builder.append(", playback=").append(playback);
    return builder.replace(0, 2, "BroadcastChannel{").append('}').toString();
  }

  public static final class Builder extends Message.Builder<BroadcastChannel, Builder> {
    public String id;

    public String name;

    public ChannelPlayback playback;

    public Builder() {
    }

    /**
     * / チャネルID
     */
    public Builder id(String id) {
      this.id = id;
      return this;
    }

    /**
     * / チャネル名
     */
    public Builder name(String name) {
      this.name = name;
      return this;
    }

    /**
     * / 再生情報
     */
    public Builder playback(ChannelPlayback playback) {
      this.playback = playback;
      return this;
    }

    @Override
    public BroadcastChannel build() {
      return new BroadcastChannel(id, name, playback, buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_BroadcastChannel extends ProtoAdapter<BroadcastChannel> {
    ProtoAdapter_BroadcastChannel() {
      super(FieldEncoding.LENGTH_DELIMITED, BroadcastChannel.class);
    }

    @Override
    public int encodedSize(BroadcastChannel value) {
      return (value.id != null ? ProtoAdapter.STRING.encodedSizeWithTag(1, value.id) : 0)
          + (value.name != null ? ProtoAdapter.STRING.encodedSizeWithTag(2, value.name) : 0)
          + (value.playback != null ? ChannelPlayback.ADAPTER.encodedSizeWithTag(3, value.playback) : 0)
          + value.unknownFields().size();
    }

    @Override
    public void encode(ProtoWriter writer, BroadcastChannel value) throws IOException {
      if (value.id != null) ProtoAdapter.STRING.encodeWithTag(writer, 1, value.id);
      if (value.name != null) ProtoAdapter.STRING.encodeWithTag(writer, 2, value.name);
      if (value.playback != null) ChannelPlayback.ADAPTER.encodeWithTag(writer, 3, value.playback);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public BroadcastChannel decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.id(ProtoAdapter.STRING.decode(reader)); break;
          case 2: builder.name(ProtoAdapter.STRING.decode(reader)); break;
          case 3: builder.playback(ChannelPlayback.ADAPTER.decode(reader)); break;
          default: {
            FieldEncoding fieldEncoding = reader.peekFieldEncoding();
            Object value = fieldEncoding.rawProtoAdapter().decode(reader);
            builder.addUnknownField(tag, fieldEncoding, value);
          }
        }
      }
      reader.endMessage(token);
      return builder.build();
    }

    @Override
    public BroadcastChannel redact(BroadcastChannel value) {
      Builder builder = value.newBuilder();
      if (builder.playback != null) builder.playback = ChannelPlayback.ADAPTER.redact(builder.playback);
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
