// Code generated by Wire protocol buffer compiler, do not edit.
// Source file: api/video.proto at 93:1
package tv.abema.protos;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.WireField;
import com.squareup.wire.internal.Internal;
import java.io.IOException;
import java.lang.Object;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import okio.ByteString;

/**
 * / ビデオシリーズ情報
 */
public final class VideoSeriesInfo extends Message<VideoSeriesInfo, VideoSeriesInfo.Builder> {
  public static final ProtoAdapter<VideoSeriesInfo> ADAPTER = new ProtoAdapter_VideoSeriesInfo();

  private static final long serialVersionUID = 0L;

  public static final String DEFAULT_ID = "";

  public static final String DEFAULT_TITLE = "";

  /**
   * / シリーズID
   */
  @WireField(
      tag = 1,
      adapter = "com.squareup.wire.ProtoAdapter#STRING"
  )
  public final String id;

  /**
   * / シリーズタイトル
   */
  @WireField(
      tag = 2,
      adapter = "com.squareup.wire.ProtoAdapter#STRING"
  )
  public final String title;

  /**
   * / ラベル情報
   */
  @WireField(
      tag = 3,
      adapter = "tv.abema.protos.VideoSeriesLabel#ADAPTER"
  )
  public final VideoSeriesLabel label;

  public VideoSeriesInfo(String id, String title, VideoSeriesLabel label) {
    this(id, title, label, ByteString.EMPTY);
  }

  public VideoSeriesInfo(String id, String title, VideoSeriesLabel label, ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    this.id = id;
    this.title = title;
    this.label = label;
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.id = id;
    builder.title = title;
    builder.label = label;
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public boolean equals(Object other) {
    if (other == this) return true;
    if (!(other instanceof VideoSeriesInfo)) return false;
    VideoSeriesInfo o = (VideoSeriesInfo) other;
    return Internal.equals(unknownFields(), o.unknownFields())
        && Internal.equals(id, o.id)
        && Internal.equals(title, o.title)
        && Internal.equals(label, o.label);
  }

  @Override
  public int hashCode() {
    int result = super.hashCode;
    if (result == 0) {
      result = unknownFields().hashCode();
      result = result * 37 + (id != null ? id.hashCode() : 0);
      result = result * 37 + (title != null ? title.hashCode() : 0);
      result = result * 37 + (label != null ? label.hashCode() : 0);
      super.hashCode = result;
    }
    return result;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    if (id != null) builder.append(", id=").append(id);
    if (title != null) builder.append(", title=").append(title);
    if (label != null) builder.append(", label=").append(label);
    return builder.replace(0, 2, "VideoSeriesInfo{").append('}').toString();
  }

  public static final class Builder extends Message.Builder<VideoSeriesInfo, Builder> {
    public String id;

    public String title;

    public VideoSeriesLabel label;

    public Builder() {
    }

    /**
     * / シリーズID
     */
    public Builder id(String id) {
      this.id = id;
      return this;
    }

    /**
     * / シリーズタイトル
     */
    public Builder title(String title) {
      this.title = title;
      return this;
    }

    /**
     * / ラベル情報
     */
    public Builder label(VideoSeriesLabel label) {
      this.label = label;
      return this;
    }

    @Override
    public VideoSeriesInfo build() {
      return new VideoSeriesInfo(id, title, label, buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_VideoSeriesInfo extends ProtoAdapter<VideoSeriesInfo> {
    ProtoAdapter_VideoSeriesInfo() {
      super(FieldEncoding.LENGTH_DELIMITED, VideoSeriesInfo.class);
    }

    @Override
    public int encodedSize(VideoSeriesInfo value) {
      return (value.id != null ? ProtoAdapter.STRING.encodedSizeWithTag(1, value.id) : 0)
          + (value.title != null ? ProtoAdapter.STRING.encodedSizeWithTag(2, value.title) : 0)
          + (value.label != null ? VideoSeriesLabel.ADAPTER.encodedSizeWithTag(3, value.label) : 0)
          + value.unknownFields().size();
    }

    @Override
    public void encode(ProtoWriter writer, VideoSeriesInfo value) throws IOException {
      if (value.id != null) ProtoAdapter.STRING.encodeWithTag(writer, 1, value.id);
      if (value.title != null) ProtoAdapter.STRING.encodeWithTag(writer, 2, value.title);
      if (value.label != null) VideoSeriesLabel.ADAPTER.encodeWithTag(writer, 3, value.label);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public VideoSeriesInfo decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.id(ProtoAdapter.STRING.decode(reader)); break;
          case 2: builder.title(ProtoAdapter.STRING.decode(reader)); break;
          case 3: builder.label(VideoSeriesLabel.ADAPTER.decode(reader)); break;
          default: {
            FieldEncoding fieldEncoding = reader.peekFieldEncoding();
            Object value = fieldEncoding.rawProtoAdapter().decode(reader);
            builder.addUnknownField(tag, fieldEncoding, value);
          }
        }
      }
      reader.endMessage(token);
      return builder.build();
    }

    @Override
    public VideoSeriesInfo redact(VideoSeriesInfo value) {
      Builder builder = value.newBuilder();
      if (builder.label != null) builder.label = VideoSeriesLabel.ADAPTER.redact(builder.label);
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
