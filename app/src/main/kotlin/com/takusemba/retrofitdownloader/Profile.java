// Code generated by Wire protocol buffer compiler, do not edit.
// Source file: api/user.proto at 47:1
package tv.abema.protos;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.WireField;
import com.squareup.wire.internal.Internal;
import java.io.IOException;
import java.lang.Long;
import java.lang.Object;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import okio.ByteString;

/**
 * Represents a profile of user
 */
public final class Profile extends Message<Profile, Profile.Builder> {
  public static final ProtoAdapter<Profile> ADAPTER = new ProtoAdapter_Profile();

  private static final long serialVersionUID = 0L;

  public static final String DEFAULT_USERID = "";

  public static final String DEFAULT_NAME = "";

  public static final Long DEFAULT_CREATEDAT = 0L;

  /**
   * / User ID
   */
  @WireField(
      tag = 1,
      adapter = "com.squareup.wire.ProtoAdapter#STRING"
  )
  public final String userId;

  /**
   * / User name
   */
  @WireField(
      tag = 2,
      adapter = "com.squareup.wire.ProtoAdapter#STRING"
  )
  public final String name;

  /**
   * / Created at (unix time)
   */
  @WireField(
      tag = 3,
      adapter = "com.squareup.wire.ProtoAdapter#INT64"
  )
  public final Long createdAt;

  public Profile(String userId, String name, Long createdAt) {
    this(userId, name, createdAt, ByteString.EMPTY);
  }

  public Profile(String userId, String name, Long createdAt, ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    this.userId = userId;
    this.name = name;
    this.createdAt = createdAt;
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.userId = userId;
    builder.name = name;
    builder.createdAt = createdAt;
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public boolean equals(Object other) {
    if (other == this) return true;
    if (!(other instanceof Profile)) return false;
    Profile o = (Profile) other;
    return Internal.equals(unknownFields(), o.unknownFields())
        && Internal.equals(userId, o.userId)
        && Internal.equals(name, o.name)
        && Internal.equals(createdAt, o.createdAt);
  }

  @Override
  public int hashCode() {
    int result = super.hashCode;
    if (result == 0) {
      result = unknownFields().hashCode();
      result = result * 37 + (userId != null ? userId.hashCode() : 0);
      result = result * 37 + (name != null ? name.hashCode() : 0);
      result = result * 37 + (createdAt != null ? createdAt.hashCode() : 0);
      super.hashCode = result;
    }
    return result;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    if (userId != null) builder.append(", userId=").append(userId);
    if (name != null) builder.append(", name=").append(name);
    if (createdAt != null) builder.append(", createdAt=").append(createdAt);
    return builder.replace(0, 2, "Profile{").append('}').toString();
  }

  public static final class Builder extends Message.Builder<Profile, Builder> {
    public String userId;

    public String name;

    public Long createdAt;

    public Builder() {
    }

    /**
     * / User ID
     */
    public Builder userId(String userId) {
      this.userId = userId;
      return this;
    }

    /**
     * / User name
     */
    public Builder name(String name) {
      this.name = name;
      return this;
    }

    /**
     * / Created at (unix time)
     */
    public Builder createdAt(Long createdAt) {
      this.createdAt = createdAt;
      return this;
    }

    @Override
    public Profile build() {
      return new Profile(userId, name, createdAt, buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_Profile extends ProtoAdapter<Profile> {
    ProtoAdapter_Profile() {
      super(FieldEncoding.LENGTH_DELIMITED, Profile.class);
    }

    @Override
    public int encodedSize(Profile value) {
      return (value.userId != null ? ProtoAdapter.STRING.encodedSizeWithTag(1, value.userId) : 0)
          + (value.name != null ? ProtoAdapter.STRING.encodedSizeWithTag(2, value.name) : 0)
          + (value.createdAt != null ? ProtoAdapter.INT64.encodedSizeWithTag(3, value.createdAt) : 0)
          + value.unknownFields().size();
    }

    @Override
    public void encode(ProtoWriter writer, Profile value) throws IOException {
      if (value.userId != null) ProtoAdapter.STRING.encodeWithTag(writer, 1, value.userId);
      if (value.name != null) ProtoAdapter.STRING.encodeWithTag(writer, 2, value.name);
      if (value.createdAt != null) ProtoAdapter.INT64.encodeWithTag(writer, 3, value.createdAt);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public Profile decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.userId(ProtoAdapter.STRING.decode(reader)); break;
          case 2: builder.name(ProtoAdapter.STRING.decode(reader)); break;
          case 3: builder.createdAt(ProtoAdapter.INT64.decode(reader)); break;
          default: {
            FieldEncoding fieldEncoding = reader.peekFieldEncoding();
            Object value = fieldEncoding.rawProtoAdapter().decode(reader);
            builder.addUnknownField(tag, fieldEncoding, value);
          }
        }
      }
      reader.endMessage(token);
      return builder.build();
    }

    @Override
    public Profile redact(Profile value) {
      Builder builder = value.newBuilder();
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
