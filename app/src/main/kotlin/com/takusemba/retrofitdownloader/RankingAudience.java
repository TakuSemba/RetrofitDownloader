// Code generated by Wire protocol buffer compiler, do not edit.
// Source file: api/ranking.proto at 28:1
package tv.abema.protos;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.WireField;
import com.squareup.wire.internal.Internal;
import java.io.IOException;
import java.lang.Long;
import java.lang.Object;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import okio.ByteString;

/**
 * / A RankingAudience has view count and reserved count.
 */
public final class RankingAudience extends Message<RankingAudience, RankingAudience.Builder> {
  public static final ProtoAdapter<RankingAudience> ADAPTER = new ProtoAdapter_RankingAudience();

  private static final long serialVersionUID = 0L;

  public static final Long DEFAULT_VIEW = 0L;

  public static final Long DEFAULT_RESERVED = 0L;

  @WireField(
      tag = 1,
      adapter = "com.squareup.wire.ProtoAdapter#INT64"
  )
  public final Long view;

  @WireField(
      tag = 2,
      adapter = "com.squareup.wire.ProtoAdapter#INT64"
  )
  public final Long reserved;

  public RankingAudience(Long view, Long reserved) {
    this(view, reserved, ByteString.EMPTY);
  }

  public RankingAudience(Long view, Long reserved, ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    this.view = view;
    this.reserved = reserved;
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.view = view;
    builder.reserved = reserved;
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public boolean equals(Object other) {
    if (other == this) return true;
    if (!(other instanceof RankingAudience)) return false;
    RankingAudience o = (RankingAudience) other;
    return Internal.equals(unknownFields(), o.unknownFields())
        && Internal.equals(view, o.view)
        && Internal.equals(reserved, o.reserved);
  }

  @Override
  public int hashCode() {
    int result = super.hashCode;
    if (result == 0) {
      result = unknownFields().hashCode();
      result = result * 37 + (view != null ? view.hashCode() : 0);
      result = result * 37 + (reserved != null ? reserved.hashCode() : 0);
      super.hashCode = result;
    }
    return result;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    if (view != null) builder.append(", view=").append(view);
    if (reserved != null) builder.append(", reserved=").append(reserved);
    return builder.replace(0, 2, "RankingAudience{").append('}').toString();
  }

  public static final class Builder extends Message.Builder<RankingAudience, Builder> {
    public Long view;

    public Long reserved;

    public Builder() {
    }

    public Builder view(Long view) {
      this.view = view;
      return this;
    }

    public Builder reserved(Long reserved) {
      this.reserved = reserved;
      return this;
    }

    @Override
    public RankingAudience build() {
      return new RankingAudience(view, reserved, buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_RankingAudience extends ProtoAdapter<RankingAudience> {
    ProtoAdapter_RankingAudience() {
      super(FieldEncoding.LENGTH_DELIMITED, RankingAudience.class);
    }

    @Override
    public int encodedSize(RankingAudience value) {
      return (value.view != null ? ProtoAdapter.INT64.encodedSizeWithTag(1, value.view) : 0)
          + (value.reserved != null ? ProtoAdapter.INT64.encodedSizeWithTag(2, value.reserved) : 0)
          + value.unknownFields().size();
    }

    @Override
    public void encode(ProtoWriter writer, RankingAudience value) throws IOException {
      if (value.view != null) ProtoAdapter.INT64.encodeWithTag(writer, 1, value.view);
      if (value.reserved != null) ProtoAdapter.INT64.encodeWithTag(writer, 2, value.reserved);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public RankingAudience decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.view(ProtoAdapter.INT64.decode(reader)); break;
          case 2: builder.reserved(ProtoAdapter.INT64.decode(reader)); break;
          default: {
            FieldEncoding fieldEncoding = reader.peekFieldEncoding();
            Object value = fieldEncoding.rawProtoAdapter().decode(reader);
            builder.addUnknownField(tag, fieldEncoding, value);
          }
        }
      }
      reader.endMessage(token);
      return builder.build();
    }

    @Override
    public RankingAudience redact(RankingAudience value) {
      Builder builder = value.newBuilder();
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
