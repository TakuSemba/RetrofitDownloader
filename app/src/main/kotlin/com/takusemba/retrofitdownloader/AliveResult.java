// Code generated by Wire protocol buffer compiler, do not edit.
// Source file: api/status.proto at 12:1
package tv.abema.protos;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.WireField;
import com.squareup.wire.internal.Internal;
import java.io.IOException;
import java.lang.Object;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import okio.ByteString;

/**
 * Represents a result of alive
 */
public final class AliveResult extends Message<AliveResult, AliveResult.Builder> {
  public static final ProtoAdapter<AliveResult> ADAPTER = new ProtoAdapter_AliveResult();

  private static final long serialVersionUID = 0L;

  public static final String DEFAULT_STATUS = "";

  /**
   * / Status "OK" or not
   */
  @WireField(
      tag = 1,
      adapter = "com.squareup.wire.ProtoAdapter#STRING"
  )
  public final String status;

  public AliveResult(String status) {
    this(status, ByteString.EMPTY);
  }

  public AliveResult(String status, ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    this.status = status;
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.status = status;
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public boolean equals(Object other) {
    if (other == this) return true;
    if (!(other instanceof AliveResult)) return false;
    AliveResult o = (AliveResult) other;
    return Internal.equals(unknownFields(), o.unknownFields())
        && Internal.equals(status, o.status);
  }

  @Override
  public int hashCode() {
    int result = super.hashCode;
    if (result == 0) {
      result = unknownFields().hashCode();
      result = result * 37 + (status != null ? status.hashCode() : 0);
      super.hashCode = result;
    }
    return result;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    if (status != null) builder.append(", status=").append(status);
    return builder.replace(0, 2, "AliveResult{").append('}').toString();
  }

  public static final class Builder extends Message.Builder<AliveResult, Builder> {
    public String status;

    public Builder() {
    }

    /**
     * / Status "OK" or not
     */
    public Builder status(String status) {
      this.status = status;
      return this;
    }

    @Override
    public AliveResult build() {
      return new AliveResult(status, buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_AliveResult extends ProtoAdapter<AliveResult> {
    ProtoAdapter_AliveResult() {
      super(FieldEncoding.LENGTH_DELIMITED, AliveResult.class);
    }

    @Override
    public int encodedSize(AliveResult value) {
      return (value.status != null ? ProtoAdapter.STRING.encodedSizeWithTag(1, value.status) : 0)
          + value.unknownFields().size();
    }

    @Override
    public void encode(ProtoWriter writer, AliveResult value) throws IOException {
      if (value.status != null) ProtoAdapter.STRING.encodeWithTag(writer, 1, value.status);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public AliveResult decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.status(ProtoAdapter.STRING.decode(reader)); break;
          default: {
            FieldEncoding fieldEncoding = reader.peekFieldEncoding();
            Object value = fieldEncoding.rawProtoAdapter().decode(reader);
            builder.addUnknownField(tag, fieldEncoding, value);
          }
        }
      }
      reader.endMessage(token);
      return builder.build();
    }

    @Override
    public AliveResult redact(AliveResult value) {
      Builder builder = value.newBuilder();
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
