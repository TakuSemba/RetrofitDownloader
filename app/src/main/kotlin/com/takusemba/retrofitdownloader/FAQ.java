// Code generated by Wire protocol buffer compiler, do not edit.
// Source file: api/faq.proto at 11:1
package tv.abema.protos;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.WireField;
import com.squareup.wire.internal.Internal;
import java.io.IOException;
import java.lang.Object;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import java.util.List;
import okio.ByteString;

/**
 * / FAQ frequently asked question
 */
public final class FAQ extends Message<FAQ, FAQ.Builder> {
  public static final ProtoAdapter<FAQ> ADAPTER = new ProtoAdapter_FAQ();

  private static final long serialVersionUID = 0L;

  /**
   * / categories
   */
  @WireField(
      tag = 1,
      adapter = "tv.abema.protos.Category#ADAPTER",
      label = WireField.Label.REPEATED
  )
  public final List<Category> categories;

  public FAQ(List<Category> categories) {
    this(categories, ByteString.EMPTY);
  }

  public FAQ(List<Category> categories, ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    this.categories = Internal.immutableCopyOf("categories", categories);
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.categories = Internal.copyOf("categories", categories);
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public boolean equals(Object other) {
    if (other == this) return true;
    if (!(other instanceof FAQ)) return false;
    FAQ o = (FAQ) other;
    return Internal.equals(unknownFields(), o.unknownFields())
        && Internal.equals(categories, o.categories);
  }

  @Override
  public int hashCode() {
    int result = super.hashCode;
    if (result == 0) {
      result = unknownFields().hashCode();
      result = result * 37 + (categories != null ? categories.hashCode() : 1);
      super.hashCode = result;
    }
    return result;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    if (categories != null) builder.append(", categories=").append(categories);
    return builder.replace(0, 2, "FAQ{").append('}').toString();
  }

  public static final class Builder extends Message.Builder<FAQ, Builder> {
    public List<Category> categories;

    public Builder() {
      categories = Internal.newMutableList();
    }

    /**
     * / categories
     */
    public Builder categories(List<Category> categories) {
      Internal.checkElementsNotNull(categories);
      this.categories = categories;
      return this;
    }

    @Override
    public FAQ build() {
      return new FAQ(categories, buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_FAQ extends ProtoAdapter<FAQ> {
    ProtoAdapter_FAQ() {
      super(FieldEncoding.LENGTH_DELIMITED, FAQ.class);
    }

    @Override
    public int encodedSize(FAQ value) {
      return Category.ADAPTER.asRepeated().encodedSizeWithTag(1, value.categories)
          + value.unknownFields().size();
    }

    @Override
    public void encode(ProtoWriter writer, FAQ value) throws IOException {
      if (value.categories != null) Category.ADAPTER.asRepeated().encodeWithTag(writer, 1, value.categories);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public FAQ decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.categories.add(Category.ADAPTER.decode(reader)); break;
          default: {
            FieldEncoding fieldEncoding = reader.peekFieldEncoding();
            Object value = fieldEncoding.rawProtoAdapter().decode(reader);
            builder.addUnknownField(tag, fieldEncoding, value);
          }
        }
      }
      reader.endMessage(token);
      return builder.build();
    }

    @Override
    public FAQ redact(FAQ value) {
      Builder builder = value.newBuilder();
      Internal.redactElements(builder.categories, Category.ADAPTER);
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
