// Code generated by Wire protocol buffer compiler, do not edit.
// Source file: api/viewing_reservation.proto at 12:1
package tv.abema.protos;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.WireField;
import com.squareup.wire.internal.Internal;
import java.io.IOException;
import java.lang.Boolean;
import java.lang.Object;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import okio.ByteString;

/**
 * / ViewingReservatinSlot represents a viewing reservation slot.
 */
public final class ViewingReservationSlot extends Message<ViewingReservationSlot, ViewingReservationSlot.Builder> {
  public static final ProtoAdapter<ViewingReservationSlot> ADAPTER = new ProtoAdapter_ViewingReservationSlot();

  private static final long serialVersionUID = 0L;

  public static final String DEFAULT_SLOTID = "";

  public static final Boolean DEFAULT_REPETITION = false;

  public static final String DEFAULT_SLOTGROUPID = "";

  /**
   * / Slot ID that has been reserved for viewing.
   */
  @WireField(
      tag = 1,
      adapter = "com.squareup.wire.ProtoAdapter#STRING"
  )
  public final String slotId;

  /**
   * / Whether to repeat.
   */
  @WireField(
      tag = 2,
      adapter = "com.squareup.wire.ProtoAdapter#BOOL"
  )
  public final Boolean repetition;

  /**
   * / Slot group ID to repeat.
   */
  @WireField(
      tag = 3,
      adapter = "com.squareup.wire.ProtoAdapter#STRING"
  )
  public final String slotGroupId;

  public ViewingReservationSlot(String slotId, Boolean repetition, String slotGroupId) {
    this(slotId, repetition, slotGroupId, ByteString.EMPTY);
  }

  public ViewingReservationSlot(String slotId, Boolean repetition, String slotGroupId, ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    this.slotId = slotId;
    this.repetition = repetition;
    this.slotGroupId = slotGroupId;
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.slotId = slotId;
    builder.repetition = repetition;
    builder.slotGroupId = slotGroupId;
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public boolean equals(Object other) {
    if (other == this) return true;
    if (!(other instanceof ViewingReservationSlot)) return false;
    ViewingReservationSlot o = (ViewingReservationSlot) other;
    return Internal.equals(unknownFields(), o.unknownFields())
        && Internal.equals(slotId, o.slotId)
        && Internal.equals(repetition, o.repetition)
        && Internal.equals(slotGroupId, o.slotGroupId);
  }

  @Override
  public int hashCode() {
    int result = super.hashCode;
    if (result == 0) {
      result = unknownFields().hashCode();
      result = result * 37 + (slotId != null ? slotId.hashCode() : 0);
      result = result * 37 + (repetition != null ? repetition.hashCode() : 0);
      result = result * 37 + (slotGroupId != null ? slotGroupId.hashCode() : 0);
      super.hashCode = result;
    }
    return result;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    if (slotId != null) builder.append(", slotId=").append(slotId);
    if (repetition != null) builder.append(", repetition=").append(repetition);
    if (slotGroupId != null) builder.append(", slotGroupId=").append(slotGroupId);
    return builder.replace(0, 2, "ViewingReservationSlot{").append('}').toString();
  }

  public static final class Builder extends Message.Builder<ViewingReservationSlot, Builder> {
    public String slotId;

    public Boolean repetition;

    public String slotGroupId;

    public Builder() {
    }

    /**
     * / Slot ID that has been reserved for viewing.
     */
    public Builder slotId(String slotId) {
      this.slotId = slotId;
      return this;
    }

    /**
     * / Whether to repeat.
     */
    public Builder repetition(Boolean repetition) {
      this.repetition = repetition;
      return this;
    }

    /**
     * / Slot group ID to repeat.
     */
    public Builder slotGroupId(String slotGroupId) {
      this.slotGroupId = slotGroupId;
      return this;
    }

    @Override
    public ViewingReservationSlot build() {
      return new ViewingReservationSlot(slotId, repetition, slotGroupId, buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_ViewingReservationSlot extends ProtoAdapter<ViewingReservationSlot> {
    ProtoAdapter_ViewingReservationSlot() {
      super(FieldEncoding.LENGTH_DELIMITED, ViewingReservationSlot.class);
    }

    @Override
    public int encodedSize(ViewingReservationSlot value) {
      return (value.slotId != null ? ProtoAdapter.STRING.encodedSizeWithTag(1, value.slotId) : 0)
          + (value.repetition != null ? ProtoAdapter.BOOL.encodedSizeWithTag(2, value.repetition) : 0)
          + (value.slotGroupId != null ? ProtoAdapter.STRING.encodedSizeWithTag(3, value.slotGroupId) : 0)
          + value.unknownFields().size();
    }

    @Override
    public void encode(ProtoWriter writer, ViewingReservationSlot value) throws IOException {
      if (value.slotId != null) ProtoAdapter.STRING.encodeWithTag(writer, 1, value.slotId);
      if (value.repetition != null) ProtoAdapter.BOOL.encodeWithTag(writer, 2, value.repetition);
      if (value.slotGroupId != null) ProtoAdapter.STRING.encodeWithTag(writer, 3, value.slotGroupId);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public ViewingReservationSlot decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.slotId(ProtoAdapter.STRING.decode(reader)); break;
          case 2: builder.repetition(ProtoAdapter.BOOL.decode(reader)); break;
          case 3: builder.slotGroupId(ProtoAdapter.STRING.decode(reader)); break;
          default: {
            FieldEncoding fieldEncoding = reader.peekFieldEncoding();
            Object value = fieldEncoding.rawProtoAdapter().decode(reader);
            builder.addUnknownField(tag, fieldEncoding, value);
          }
        }
      }
      reader.endMessage(token);
      return builder.build();
    }

    @Override
    public ViewingReservationSlot redact(ViewingReservationSlot value) {
      Builder builder = value.newBuilder();
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
