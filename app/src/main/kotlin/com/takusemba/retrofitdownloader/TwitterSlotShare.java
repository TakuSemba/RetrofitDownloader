// Code generated by Wire protocol buffer compiler, do not edit.
// Source file: api/share.proto at 31:1
package tv.abema.protos;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.WireField;
import com.squareup.wire.internal.Internal;
import java.io.IOException;
import java.lang.Object;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import okio.ByteString;

/**
 * / Represents a dataset for sharing on Twitter
 */
public final class TwitterSlotShare extends Message<TwitterSlotShare, TwitterSlotShare.Builder> {
  public static final ProtoAdapter<TwitterSlotShare> ADAPTER = new ProtoAdapter_TwitterSlotShare();

  private static final long serialVersionUID = 0L;

  public static final String DEFAULT_ACCESSTOKEN = "";

  public static final String DEFAULT_ACCESSTOKENSECRET = "";

  /**
   * / Access token of Twitter
   */
  @WireField(
      tag = 1,
      adapter = "com.squareup.wire.ProtoAdapter#STRING"
  )
  public final String accessToken;

  /**
   * / Access token secret of Twitter
   */
  @WireField(
      tag = 2,
      adapter = "com.squareup.wire.ProtoAdapter#STRING"
  )
  public final String accessTokenSecret;

  public TwitterSlotShare(String accessToken, String accessTokenSecret) {
    this(accessToken, accessTokenSecret, ByteString.EMPTY);
  }

  public TwitterSlotShare(String accessToken, String accessTokenSecret, ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    this.accessToken = accessToken;
    this.accessTokenSecret = accessTokenSecret;
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.accessToken = accessToken;
    builder.accessTokenSecret = accessTokenSecret;
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public boolean equals(Object other) {
    if (other == this) return true;
    if (!(other instanceof TwitterSlotShare)) return false;
    TwitterSlotShare o = (TwitterSlotShare) other;
    return Internal.equals(unknownFields(), o.unknownFields())
        && Internal.equals(accessToken, o.accessToken)
        && Internal.equals(accessTokenSecret, o.accessTokenSecret);
  }

  @Override
  public int hashCode() {
    int result = super.hashCode;
    if (result == 0) {
      result = unknownFields().hashCode();
      result = result * 37 + (accessToken != null ? accessToken.hashCode() : 0);
      result = result * 37 + (accessTokenSecret != null ? accessTokenSecret.hashCode() : 0);
      super.hashCode = result;
    }
    return result;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    if (accessToken != null) builder.append(", accessToken=").append(accessToken);
    if (accessTokenSecret != null) builder.append(", accessTokenSecret=").append(accessTokenSecret);
    return builder.replace(0, 2, "TwitterSlotShare{").append('}').toString();
  }

  public static final class Builder extends Message.Builder<TwitterSlotShare, Builder> {
    public String accessToken;

    public String accessTokenSecret;

    public Builder() {
    }

    /**
     * / Access token of Twitter
     */
    public Builder accessToken(String accessToken) {
      this.accessToken = accessToken;
      return this;
    }

    /**
     * / Access token secret of Twitter
     */
    public Builder accessTokenSecret(String accessTokenSecret) {
      this.accessTokenSecret = accessTokenSecret;
      return this;
    }

    @Override
    public TwitterSlotShare build() {
      return new TwitterSlotShare(accessToken, accessTokenSecret, buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_TwitterSlotShare extends ProtoAdapter<TwitterSlotShare> {
    ProtoAdapter_TwitterSlotShare() {
      super(FieldEncoding.LENGTH_DELIMITED, TwitterSlotShare.class);
    }

    @Override
    public int encodedSize(TwitterSlotShare value) {
      return (value.accessToken != null ? ProtoAdapter.STRING.encodedSizeWithTag(1, value.accessToken) : 0)
          + (value.accessTokenSecret != null ? ProtoAdapter.STRING.encodedSizeWithTag(2, value.accessTokenSecret) : 0)
          + value.unknownFields().size();
    }

    @Override
    public void encode(ProtoWriter writer, TwitterSlotShare value) throws IOException {
      if (value.accessToken != null) ProtoAdapter.STRING.encodeWithTag(writer, 1, value.accessToken);
      if (value.accessTokenSecret != null) ProtoAdapter.STRING.encodeWithTag(writer, 2, value.accessTokenSecret);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public TwitterSlotShare decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.accessToken(ProtoAdapter.STRING.decode(reader)); break;
          case 2: builder.accessTokenSecret(ProtoAdapter.STRING.decode(reader)); break;
          default: {
            FieldEncoding fieldEncoding = reader.peekFieldEncoding();
            Object value = fieldEncoding.rawProtoAdapter().decode(reader);
            builder.addUnknownField(tag, fieldEncoding, value);
          }
        }
      }
      reader.endMessage(token);
      return builder.build();
    }

    @Override
    public TwitterSlotShare redact(TwitterSlotShare value) {
      Builder builder = value.newBuilder();
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
