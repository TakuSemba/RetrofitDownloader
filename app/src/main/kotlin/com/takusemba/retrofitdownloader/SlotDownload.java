// Code generated by Wire protocol buffer compiler, do not edit.
// Source file: api/media.proto at 316:1
package tv.abema.protos;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.WireField;
import com.squareup.wire.internal.Internal;
import java.io.IOException;
import java.lang.Boolean;
import java.lang.Object;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import okio.ByteString;

/**
 * /ダウンロード放送枠(タイムシフト)
 */
public final class SlotDownload extends Message<SlotDownload, SlotDownload.Builder> {
  public static final ProtoAdapter<SlotDownload> ADAPTER = new ProtoAdapter_SlotDownload();

  private static final long serialVersionUID = 0L;

  public static final Boolean DEFAULT_ENABLE = false;

  /**
   * /ダウンロード可否(契約＆契約準拠)
   */
  @WireField(
      tag = 1,
      adapter = "com.squareup.wire.ProtoAdapter#BOOL"
  )
  public final Boolean enable;

  public SlotDownload(Boolean enable) {
    this(enable, ByteString.EMPTY);
  }

  public SlotDownload(Boolean enable, ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    this.enable = enable;
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.enable = enable;
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public boolean equals(Object other) {
    if (other == this) return true;
    if (!(other instanceof SlotDownload)) return false;
    SlotDownload o = (SlotDownload) other;
    return Internal.equals(unknownFields(), o.unknownFields())
        && Internal.equals(enable, o.enable);
  }

  @Override
  public int hashCode() {
    int result = super.hashCode;
    if (result == 0) {
      result = unknownFields().hashCode();
      result = result * 37 + (enable != null ? enable.hashCode() : 0);
      super.hashCode = result;
    }
    return result;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    if (enable != null) builder.append(", enable=").append(enable);
    return builder.replace(0, 2, "SlotDownload{").append('}').toString();
  }

  public static final class Builder extends Message.Builder<SlotDownload, Builder> {
    public Boolean enable;

    public Builder() {
    }

    /**
     * /ダウンロード可否(契約＆契約準拠)
     */
    public Builder enable(Boolean enable) {
      this.enable = enable;
      return this;
    }

    @Override
    public SlotDownload build() {
      return new SlotDownload(enable, buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_SlotDownload extends ProtoAdapter<SlotDownload> {
    ProtoAdapter_SlotDownload() {
      super(FieldEncoding.LENGTH_DELIMITED, SlotDownload.class);
    }

    @Override
    public int encodedSize(SlotDownload value) {
      return (value.enable != null ? ProtoAdapter.BOOL.encodedSizeWithTag(1, value.enable) : 0)
          + value.unknownFields().size();
    }

    @Override
    public void encode(ProtoWriter writer, SlotDownload value) throws IOException {
      if (value.enable != null) ProtoAdapter.BOOL.encodeWithTag(writer, 1, value.enable);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public SlotDownload decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.enable(ProtoAdapter.BOOL.decode(reader)); break;
          default: {
            FieldEncoding fieldEncoding = reader.peekFieldEncoding();
            Object value = fieldEncoding.rawProtoAdapter().decode(reader);
            builder.addUnknownField(tag, fieldEncoding, value);
          }
        }
      }
      reader.endMessage(token);
      return builder.build();
    }

    @Override
    public SlotDownload redact(SlotDownload value) {
      Builder builder = value.newBuilder();
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
