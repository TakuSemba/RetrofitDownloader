// Code generated by Wire protocol buffer compiler, do not edit.
// Source file: api/question.proto at 42:1
package tv.abema.protos;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.WireField;
import com.squareup.wire.internal.Internal;
import java.io.IOException;
import java.lang.Float;
import java.lang.Integer;
import java.lang.Object;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import okio.ByteString;

/**
 * / 選択肢毎の結果
 */
public final class ResultChoice extends Message<ResultChoice, ResultChoice.Builder> {
  public static final ProtoAdapter<ResultChoice> ADAPTER = new ProtoAdapter_ResultChoice();

  private static final long serialVersionUID = 0L;

  public static final Integer DEFAULT_NUMBER = 0;

  public static final Float DEFAULT_PERCENTAGE = 0.0f;

  /**
   * / 選択肢番号
   */
  @WireField(
      tag = 1,
      adapter = "com.squareup.wire.ProtoAdapter#INT32"
  )
  public final Integer number;

  /**
   * / 獲得数の割合(％)
   */
  @WireField(
      tag = 2,
      adapter = "com.squareup.wire.ProtoAdapter#FLOAT"
  )
  public final Float percentage;

  public ResultChoice(Integer number, Float percentage) {
    this(number, percentage, ByteString.EMPTY);
  }

  public ResultChoice(Integer number, Float percentage, ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    this.number = number;
    this.percentage = percentage;
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.number = number;
    builder.percentage = percentage;
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public boolean equals(Object other) {
    if (other == this) return true;
    if (!(other instanceof ResultChoice)) return false;
    ResultChoice o = (ResultChoice) other;
    return Internal.equals(unknownFields(), o.unknownFields())
        && Internal.equals(number, o.number)
        && Internal.equals(percentage, o.percentage);
  }

  @Override
  public int hashCode() {
    int result = super.hashCode;
    if (result == 0) {
      result = unknownFields().hashCode();
      result = result * 37 + (number != null ? number.hashCode() : 0);
      result = result * 37 + (percentage != null ? percentage.hashCode() : 0);
      super.hashCode = result;
    }
    return result;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    if (number != null) builder.append(", number=").append(number);
    if (percentage != null) builder.append(", percentage=").append(percentage);
    return builder.replace(0, 2, "ResultChoice{").append('}').toString();
  }

  public static final class Builder extends Message.Builder<ResultChoice, Builder> {
    public Integer number;

    public Float percentage;

    public Builder() {
    }

    /**
     * / 選択肢番号
     */
    public Builder number(Integer number) {
      this.number = number;
      return this;
    }

    /**
     * / 獲得数の割合(％)
     */
    public Builder percentage(Float percentage) {
      this.percentage = percentage;
      return this;
    }

    @Override
    public ResultChoice build() {
      return new ResultChoice(number, percentage, buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_ResultChoice extends ProtoAdapter<ResultChoice> {
    ProtoAdapter_ResultChoice() {
      super(FieldEncoding.LENGTH_DELIMITED, ResultChoice.class);
    }

    @Override
    public int encodedSize(ResultChoice value) {
      return (value.number != null ? ProtoAdapter.INT32.encodedSizeWithTag(1, value.number) : 0)
          + (value.percentage != null ? ProtoAdapter.FLOAT.encodedSizeWithTag(2, value.percentage) : 0)
          + value.unknownFields().size();
    }

    @Override
    public void encode(ProtoWriter writer, ResultChoice value) throws IOException {
      if (value.number != null) ProtoAdapter.INT32.encodeWithTag(writer, 1, value.number);
      if (value.percentage != null) ProtoAdapter.FLOAT.encodeWithTag(writer, 2, value.percentage);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public ResultChoice decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.number(ProtoAdapter.INT32.decode(reader)); break;
          case 2: builder.percentage(ProtoAdapter.FLOAT.decode(reader)); break;
          default: {
            FieldEncoding fieldEncoding = reader.peekFieldEncoding();
            Object value = fieldEncoding.rawProtoAdapter().decode(reader);
            builder.addUnknownField(tag, fieldEncoding, value);
          }
        }
      }
      reader.endMessage(token);
      return builder.build();
    }

    @Override
    public ResultChoice redact(ResultChoice value) {
      Builder builder = value.newBuilder();
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
