// Code generated by Wire protocol buffer compiler, do not edit.
// Source file: api/user.proto at 95:1
package tv.abema.protos;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.WireField;
import com.squareup.wire.internal.Internal;
import java.io.IOException;
import java.lang.Object;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import java.util.List;
import okio.ByteString;

/**
 * Represents a response of get user
 */
public final class GetUserResponse extends Message<GetUserResponse, GetUserResponse.Builder> {
  public static final ProtoAdapter<GetUserResponse> ADAPTER = new ProtoAdapter_GetUserResponse();

  private static final long serialVersionUID = 0L;

  /**
   * / Target user profile
   */
  @WireField(
      tag = 1,
      adapter = "tv.abema.protos.Profile#ADAPTER"
  )
  public final Profile profile;

  /**
   * / 購読情報
   */
  @WireField(
      tag = 2,
      adapter = "tv.abema.protos.UserSubscription#ADAPTER",
      label = WireField.Label.REPEATED
  )
  public final List<UserSubscription> subscriptions;

  public GetUserResponse(Profile profile, List<UserSubscription> subscriptions) {
    this(profile, subscriptions, ByteString.EMPTY);
  }

  public GetUserResponse(Profile profile, List<UserSubscription> subscriptions, ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    this.profile = profile;
    this.subscriptions = Internal.immutableCopyOf("subscriptions", subscriptions);
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.profile = profile;
    builder.subscriptions = Internal.copyOf("subscriptions", subscriptions);
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public boolean equals(Object other) {
    if (other == this) return true;
    if (!(other instanceof GetUserResponse)) return false;
    GetUserResponse o = (GetUserResponse) other;
    return Internal.equals(unknownFields(), o.unknownFields())
        && Internal.equals(profile, o.profile)
        && Internal.equals(subscriptions, o.subscriptions);
  }

  @Override
  public int hashCode() {
    int result = super.hashCode;
    if (result == 0) {
      result = unknownFields().hashCode();
      result = result * 37 + (profile != null ? profile.hashCode() : 0);
      result = result * 37 + (subscriptions != null ? subscriptions.hashCode() : 1);
      super.hashCode = result;
    }
    return result;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    if (profile != null) builder.append(", profile=").append(profile);
    if (subscriptions != null) builder.append(", subscriptions=").append(subscriptions);
    return builder.replace(0, 2, "GetUserResponse{").append('}').toString();
  }

  public static final class Builder extends Message.Builder<GetUserResponse, Builder> {
    public Profile profile;

    public List<UserSubscription> subscriptions;

    public Builder() {
      subscriptions = Internal.newMutableList();
    }

    /**
     * / Target user profile
     */
    public Builder profile(Profile profile) {
      this.profile = profile;
      return this;
    }

    /**
     * / 購読情報
     */
    public Builder subscriptions(List<UserSubscription> subscriptions) {
      Internal.checkElementsNotNull(subscriptions);
      this.subscriptions = subscriptions;
      return this;
    }

    @Override
    public GetUserResponse build() {
      return new GetUserResponse(profile, subscriptions, buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_GetUserResponse extends ProtoAdapter<GetUserResponse> {
    ProtoAdapter_GetUserResponse() {
      super(FieldEncoding.LENGTH_DELIMITED, GetUserResponse.class);
    }

    @Override
    public int encodedSize(GetUserResponse value) {
      return (value.profile != null ? Profile.ADAPTER.encodedSizeWithTag(1, value.profile) : 0)
          + UserSubscription.ADAPTER.asRepeated().encodedSizeWithTag(2, value.subscriptions)
          + value.unknownFields().size();
    }

    @Override
    public void encode(ProtoWriter writer, GetUserResponse value) throws IOException {
      if (value.profile != null) Profile.ADAPTER.encodeWithTag(writer, 1, value.profile);
      if (value.subscriptions != null) UserSubscription.ADAPTER.asRepeated().encodeWithTag(writer, 2, value.subscriptions);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public GetUserResponse decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.profile(Profile.ADAPTER.decode(reader)); break;
          case 2: builder.subscriptions.add(UserSubscription.ADAPTER.decode(reader)); break;
          default: {
            FieldEncoding fieldEncoding = reader.peekFieldEncoding();
            Object value = fieldEncoding.rawProtoAdapter().decode(reader);
            builder.addUnknownField(tag, fieldEncoding, value);
          }
        }
      }
      reader.endMessage(token);
      return builder.build();
    }

    @Override
    public GetUserResponse redact(GetUserResponse value) {
      Builder builder = value.newBuilder();
      if (builder.profile != null) builder.profile = Profile.ADAPTER.redact(builder.profile);
      Internal.redactElements(builder.subscriptions, UserSubscription.ADAPTER);
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
