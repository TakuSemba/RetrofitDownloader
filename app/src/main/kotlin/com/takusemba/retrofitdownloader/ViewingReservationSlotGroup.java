// Code generated by Wire protocol buffer compiler, do not edit.
// Source file: api/viewing_reservation.proto at 19:1
package tv.abema.protos;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.WireField;
import com.squareup.wire.internal.Internal;
import java.io.IOException;
import java.lang.Object;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import java.util.List;
import okio.ByteString;

/**
 * / ViewingReservationSlotGroup represents a viewing reservation slotGroup.
 */
public final class ViewingReservationSlotGroup extends Message<ViewingReservationSlotGroup, ViewingReservationSlotGroup.Builder> {
  public static final ProtoAdapter<ViewingReservationSlotGroup> ADAPTER = new ProtoAdapter_ViewingReservationSlotGroup();

  private static final long serialVersionUID = 0L;

  public static final String DEFAULT_ID = "";

  /**
   * / SlotGroup ID that has been reserved for viewing.
   */
  @WireField(
      tag = 1,
      adapter = "com.squareup.wire.ProtoAdapter#STRING"
  )
  public final String id;

  /**
   * / SlotID list that belong to slot group.
   */
  @WireField(
      tag = 2,
      adapter = "com.squareup.wire.ProtoAdapter#STRING",
      label = WireField.Label.REPEATED
  )
  public final List<String> slotIds;

  public ViewingReservationSlotGroup(String id, List<String> slotIds) {
    this(id, slotIds, ByteString.EMPTY);
  }

  public ViewingReservationSlotGroup(String id, List<String> slotIds, ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    this.id = id;
    this.slotIds = Internal.immutableCopyOf("slotIds", slotIds);
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.id = id;
    builder.slotIds = Internal.copyOf("slotIds", slotIds);
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public boolean equals(Object other) {
    if (other == this) return true;
    if (!(other instanceof ViewingReservationSlotGroup)) return false;
    ViewingReservationSlotGroup o = (ViewingReservationSlotGroup) other;
    return Internal.equals(unknownFields(), o.unknownFields())
        && Internal.equals(id, o.id)
        && Internal.equals(slotIds, o.slotIds);
  }

  @Override
  public int hashCode() {
    int result = super.hashCode;
    if (result == 0) {
      result = unknownFields().hashCode();
      result = result * 37 + (id != null ? id.hashCode() : 0);
      result = result * 37 + (slotIds != null ? slotIds.hashCode() : 1);
      super.hashCode = result;
    }
    return result;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    if (id != null) builder.append(", id=").append(id);
    if (slotIds != null) builder.append(", slotIds=").append(slotIds);
    return builder.replace(0, 2, "ViewingReservationSlotGroup{").append('}').toString();
  }

  public static final class Builder extends Message.Builder<ViewingReservationSlotGroup, Builder> {
    public String id;

    public List<String> slotIds;

    public Builder() {
      slotIds = Internal.newMutableList();
    }

    /**
     * / SlotGroup ID that has been reserved for viewing.
     */
    public Builder id(String id) {
      this.id = id;
      return this;
    }

    /**
     * / SlotID list that belong to slot group.
     */
    public Builder slotIds(List<String> slotIds) {
      Internal.checkElementsNotNull(slotIds);
      this.slotIds = slotIds;
      return this;
    }

    @Override
    public ViewingReservationSlotGroup build() {
      return new ViewingReservationSlotGroup(id, slotIds, buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_ViewingReservationSlotGroup extends ProtoAdapter<ViewingReservationSlotGroup> {
    ProtoAdapter_ViewingReservationSlotGroup() {
      super(FieldEncoding.LENGTH_DELIMITED, ViewingReservationSlotGroup.class);
    }

    @Override
    public int encodedSize(ViewingReservationSlotGroup value) {
      return (value.id != null ? ProtoAdapter.STRING.encodedSizeWithTag(1, value.id) : 0)
          + ProtoAdapter.STRING.asRepeated().encodedSizeWithTag(2, value.slotIds)
          + value.unknownFields().size();
    }

    @Override
    public void encode(ProtoWriter writer, ViewingReservationSlotGroup value) throws IOException {
      if (value.id != null) ProtoAdapter.STRING.encodeWithTag(writer, 1, value.id);
      if (value.slotIds != null) ProtoAdapter.STRING.asRepeated().encodeWithTag(writer, 2, value.slotIds);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public ViewingReservationSlotGroup decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.id(ProtoAdapter.STRING.decode(reader)); break;
          case 2: builder.slotIds.add(ProtoAdapter.STRING.decode(reader)); break;
          default: {
            FieldEncoding fieldEncoding = reader.peekFieldEncoding();
            Object value = fieldEncoding.rawProtoAdapter().decode(reader);
            builder.addUnknownField(tag, fieldEncoding, value);
          }
        }
      }
      reader.endMessage(token);
      return builder.build();
    }

    @Override
    public ViewingReservationSlotGroup redact(ViewingReservationSlotGroup value) {
      Builder builder = value.newBuilder();
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
