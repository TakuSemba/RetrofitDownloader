// Code generated by Wire protocol buffer compiler, do not edit.
// Source file: api/business_contact.proto at 23:1
package tv.abema.protos;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.WireField;
import com.squareup.wire.internal.Internal;
import java.io.IOException;
import java.lang.Object;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import okio.ByteString;

/**
 * / 法人向けお問い合わせ情報取得Response
 */
public final class GetBusinessContactResponse extends Message<GetBusinessContactResponse, GetBusinessContactResponse.Builder> {
  public static final ProtoAdapter<GetBusinessContactResponse> ADAPTER = new ProtoAdapter_GetBusinessContactResponse();

  private static final long serialVersionUID = 0L;

  /**
   * / 法人向けお問合わせ
   */
  @WireField(
      tag = 1,
      adapter = "tv.abema.protos.BusinessContact#ADAPTER"
  )
  public final BusinessContact businessContact;

  public GetBusinessContactResponse(BusinessContact businessContact) {
    this(businessContact, ByteString.EMPTY);
  }

  public GetBusinessContactResponse(BusinessContact businessContact, ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    this.businessContact = businessContact;
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.businessContact = businessContact;
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public boolean equals(Object other) {
    if (other == this) return true;
    if (!(other instanceof GetBusinessContactResponse)) return false;
    GetBusinessContactResponse o = (GetBusinessContactResponse) other;
    return Internal.equals(unknownFields(), o.unknownFields())
        && Internal.equals(businessContact, o.businessContact);
  }

  @Override
  public int hashCode() {
    int result = super.hashCode;
    if (result == 0) {
      result = unknownFields().hashCode();
      result = result * 37 + (businessContact != null ? businessContact.hashCode() : 0);
      super.hashCode = result;
    }
    return result;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    if (businessContact != null) builder.append(", businessContact=").append(businessContact);
    return builder.replace(0, 2, "GetBusinessContactResponse{").append('}').toString();
  }

  public static final class Builder extends Message.Builder<GetBusinessContactResponse, Builder> {
    public BusinessContact businessContact;

    public Builder() {
    }

    /**
     * / 法人向けお問合わせ
     */
    public Builder businessContact(BusinessContact businessContact) {
      this.businessContact = businessContact;
      return this;
    }

    @Override
    public GetBusinessContactResponse build() {
      return new GetBusinessContactResponse(businessContact, buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_GetBusinessContactResponse extends ProtoAdapter<GetBusinessContactResponse> {
    ProtoAdapter_GetBusinessContactResponse() {
      super(FieldEncoding.LENGTH_DELIMITED, GetBusinessContactResponse.class);
    }

    @Override
    public int encodedSize(GetBusinessContactResponse value) {
      return (value.businessContact != null ? BusinessContact.ADAPTER.encodedSizeWithTag(1, value.businessContact) : 0)
          + value.unknownFields().size();
    }

    @Override
    public void encode(ProtoWriter writer, GetBusinessContactResponse value) throws IOException {
      if (value.businessContact != null) BusinessContact.ADAPTER.encodeWithTag(writer, 1, value.businessContact);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public GetBusinessContactResponse decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.businessContact(BusinessContact.ADAPTER.decode(reader)); break;
          default: {
            FieldEncoding fieldEncoding = reader.peekFieldEncoding();
            Object value = fieldEncoding.rawProtoAdapter().decode(reader);
            builder.addUnknownField(tag, fieldEncoding, value);
          }
        }
      }
      reader.endMessage(token);
      return builder.build();
    }

    @Override
    public GetBusinessContactResponse redact(GetBusinessContactResponse value) {
      Builder builder = value.newBuilder();
      if (builder.businessContact != null) builder.businessContact = BusinessContact.ADAPTER.redact(builder.businessContact);
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
