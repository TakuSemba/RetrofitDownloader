// Code generated by Wire protocol buffer compiler, do not edit.
// Source file: api/broadcast_slots.proto at 69:1
package tv.abema.protos;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.WireField;
import com.squareup.wire.internal.Internal;
import java.io.IOException;
import java.lang.Object;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import okio.ByteString;

public final class SocialShares extends Message<SocialShares, SocialShares.Builder> {
  public static final ProtoAdapter<SocialShares> ADAPTER = new ProtoAdapter_SocialShares();

  private static final long serialVersionUID = 0L;

  /**
   * / twitter用シェア情報
   */
  @WireField(
      tag = 1,
      adapter = "tv.abema.protos.SocialShares$Twitter#ADAPTER"
  )
  public final Twitter twitter;

  /**
   * / facebook用シェア情報
   */
  @WireField(
      tag = 2,
      adapter = "tv.abema.protos.SocialShares$Facebook#ADAPTER"
  )
  public final Facebook facebook;

  /**
   * / google用シェア情報
   */
  @WireField(
      tag = 3,
      adapter = "tv.abema.protos.SocialShares$Google#ADAPTER"
  )
  public final Google google;

  /**
   * / LINE用シェア情報
   */
  @WireField(
      tag = 4,
      adapter = "tv.abema.protos.SocialShares$Line#ADAPTER"
  )
  public final Line line;

  /**
   * / その他のリンク情報
   */
  @WireField(
      tag = 5,
      adapter = "tv.abema.protos.SocialShares$Others#ADAPTER"
  )
  public final Others others;

  public SocialShares(Twitter twitter, Facebook facebook, Google google, Line line, Others others) {
    this(twitter, facebook, google, line, others, ByteString.EMPTY);
  }

  public SocialShares(Twitter twitter, Facebook facebook, Google google, Line line, Others others, ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    this.twitter = twitter;
    this.facebook = facebook;
    this.google = google;
    this.line = line;
    this.others = others;
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.twitter = twitter;
    builder.facebook = facebook;
    builder.google = google;
    builder.line = line;
    builder.others = others;
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public boolean equals(Object other) {
    if (other == this) return true;
    if (!(other instanceof SocialShares)) return false;
    SocialShares o = (SocialShares) other;
    return Internal.equals(unknownFields(), o.unknownFields())
        && Internal.equals(twitter, o.twitter)
        && Internal.equals(facebook, o.facebook)
        && Internal.equals(google, o.google)
        && Internal.equals(line, o.line)
        && Internal.equals(others, o.others);
  }

  @Override
  public int hashCode() {
    int result = super.hashCode;
    if (result == 0) {
      result = unknownFields().hashCode();
      result = result * 37 + (twitter != null ? twitter.hashCode() : 0);
      result = result * 37 + (facebook != null ? facebook.hashCode() : 0);
      result = result * 37 + (google != null ? google.hashCode() : 0);
      result = result * 37 + (line != null ? line.hashCode() : 0);
      result = result * 37 + (others != null ? others.hashCode() : 0);
      super.hashCode = result;
    }
    return result;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    if (twitter != null) builder.append(", twitter=").append(twitter);
    if (facebook != null) builder.append(", facebook=").append(facebook);
    if (google != null) builder.append(", google=").append(google);
    if (line != null) builder.append(", line=").append(line);
    if (others != null) builder.append(", others=").append(others);
    return builder.replace(0, 2, "SocialShares{").append('}').toString();
  }

  public static final class Builder extends Message.Builder<SocialShares, Builder> {
    public Twitter twitter;

    public Facebook facebook;

    public Google google;

    public Line line;

    public Others others;

    public Builder() {
    }

    /**
     * / twitter用シェア情報
     */
    public Builder twitter(Twitter twitter) {
      this.twitter = twitter;
      return this;
    }

    /**
     * / facebook用シェア情報
     */
    public Builder facebook(Facebook facebook) {
      this.facebook = facebook;
      return this;
    }

    /**
     * / google用シェア情報
     */
    public Builder google(Google google) {
      this.google = google;
      return this;
    }

    /**
     * / LINE用シェア情報
     */
    public Builder line(Line line) {
      this.line = line;
      return this;
    }

    /**
     * / その他のリンク情報
     */
    public Builder others(Others others) {
      this.others = others;
      return this;
    }

    @Override
    public SocialShares build() {
      return new SocialShares(twitter, facebook, google, line, others, buildUnknownFields());
    }
  }

  public static final class Twitter extends Message<Twitter, Twitter.Builder> {
    public static final ProtoAdapter<Twitter> ADAPTER = new ProtoAdapter_Twitter();

    private static final long serialVersionUID = 0L;

    public static final String DEFAULT_LINK = "";

    public static final String DEFAULT_HASHTAG = "";

    public static final String DEFAULT_SCREEN = "";

    /**
     * / リンク
     */
    @WireField(
        tag = 1,
        adapter = "com.squareup.wire.ProtoAdapter#STRING"
    )
    public final String link;

    /**
     * / ハッシュタグ
     */
    @WireField(
        tag = 2,
        adapter = "com.squareup.wire.ProtoAdapter#STRING"
    )
    public final String hashtag;

    /**
     * / 画像シェア用リンク（許可されている場合のみ）
     */
    @WireField(
        tag = 3,
        adapter = "com.squareup.wire.ProtoAdapter#STRING"
    )
    public final String screen;

    public Twitter(String link, String hashtag, String screen) {
      this(link, hashtag, screen, ByteString.EMPTY);
    }

    public Twitter(String link, String hashtag, String screen, ByteString unknownFields) {
      super(ADAPTER, unknownFields);
      this.link = link;
      this.hashtag = hashtag;
      this.screen = screen;
    }

    @Override
    public Builder newBuilder() {
      Builder builder = new Builder();
      builder.link = link;
      builder.hashtag = hashtag;
      builder.screen = screen;
      builder.addUnknownFields(unknownFields());
      return builder;
    }

    @Override
    public boolean equals(Object other) {
      if (other == this) return true;
      if (!(other instanceof Twitter)) return false;
      Twitter o = (Twitter) other;
      return Internal.equals(unknownFields(), o.unknownFields())
          && Internal.equals(link, o.link)
          && Internal.equals(hashtag, o.hashtag)
          && Internal.equals(screen, o.screen);
    }

    @Override
    public int hashCode() {
      int result = super.hashCode;
      if (result == 0) {
        result = unknownFields().hashCode();
        result = result * 37 + (link != null ? link.hashCode() : 0);
        result = result * 37 + (hashtag != null ? hashtag.hashCode() : 0);
        result = result * 37 + (screen != null ? screen.hashCode() : 0);
        super.hashCode = result;
      }
      return result;
    }

    @Override
    public String toString() {
      StringBuilder builder = new StringBuilder();
      if (link != null) builder.append(", link=").append(link);
      if (hashtag != null) builder.append(", hashtag=").append(hashtag);
      if (screen != null) builder.append(", screen=").append(screen);
      return builder.replace(0, 2, "Twitter{").append('}').toString();
    }

    public static final class Builder extends Message.Builder<Twitter, Builder> {
      public String link;

      public String hashtag;

      public String screen;

      public Builder() {
      }

      /**
       * / リンク
       */
      public Builder link(String link) {
        this.link = link;
        return this;
      }

      /**
       * / ハッシュタグ
       */
      public Builder hashtag(String hashtag) {
        this.hashtag = hashtag;
        return this;
      }

      /**
       * / 画像シェア用リンク（許可されている場合のみ）
       */
      public Builder screen(String screen) {
        this.screen = screen;
        return this;
      }

      @Override
      public Twitter build() {
        return new Twitter(link, hashtag, screen, buildUnknownFields());
      }
    }

    private static final class ProtoAdapter_Twitter extends ProtoAdapter<Twitter> {
      ProtoAdapter_Twitter() {
        super(FieldEncoding.LENGTH_DELIMITED, Twitter.class);
      }

      @Override
      public int encodedSize(Twitter value) {
        return (value.link != null ? ProtoAdapter.STRING.encodedSizeWithTag(1, value.link) : 0)
            + (value.hashtag != null ? ProtoAdapter.STRING.encodedSizeWithTag(2, value.hashtag) : 0)
            + (value.screen != null ? ProtoAdapter.STRING.encodedSizeWithTag(3, value.screen) : 0)
            + value.unknownFields().size();
      }

      @Override
      public void encode(ProtoWriter writer, Twitter value) throws IOException {
        if (value.link != null) ProtoAdapter.STRING.encodeWithTag(writer, 1, value.link);
        if (value.hashtag != null) ProtoAdapter.STRING.encodeWithTag(writer, 2, value.hashtag);
        if (value.screen != null) ProtoAdapter.STRING.encodeWithTag(writer, 3, value.screen);
        writer.writeBytes(value.unknownFields());
      }

      @Override
      public Twitter decode(ProtoReader reader) throws IOException {
        Builder builder = new Builder();
        long token = reader.beginMessage();
        for (int tag; (tag = reader.nextTag()) != -1;) {
          switch (tag) {
            case 1: builder.link(ProtoAdapter.STRING.decode(reader)); break;
            case 2: builder.hashtag(ProtoAdapter.STRING.decode(reader)); break;
            case 3: builder.screen(ProtoAdapter.STRING.decode(reader)); break;
            default: {
              FieldEncoding fieldEncoding = reader.peekFieldEncoding();
              Object value = fieldEncoding.rawProtoAdapter().decode(reader);
              builder.addUnknownField(tag, fieldEncoding, value);
            }
          }
        }
        reader.endMessage(token);
        return builder.build();
      }

      @Override
      public Twitter redact(Twitter value) {
        Builder builder = value.newBuilder();
        builder.clearUnknownFields();
        return builder.build();
      }
    }
  }

  public static final class Facebook extends Message<Facebook, Facebook.Builder> {
    public static final ProtoAdapter<Facebook> ADAPTER = new ProtoAdapter_Facebook();

    private static final long serialVersionUID = 0L;

    public static final String DEFAULT_LINK = "";

    /**
     * / リンク
     */
    @WireField(
        tag = 1,
        adapter = "com.squareup.wire.ProtoAdapter#STRING"
    )
    public final String link;

    public Facebook(String link) {
      this(link, ByteString.EMPTY);
    }

    public Facebook(String link, ByteString unknownFields) {
      super(ADAPTER, unknownFields);
      this.link = link;
    }

    @Override
    public Builder newBuilder() {
      Builder builder = new Builder();
      builder.link = link;
      builder.addUnknownFields(unknownFields());
      return builder;
    }

    @Override
    public boolean equals(Object other) {
      if (other == this) return true;
      if (!(other instanceof Facebook)) return false;
      Facebook o = (Facebook) other;
      return Internal.equals(unknownFields(), o.unknownFields())
          && Internal.equals(link, o.link);
    }

    @Override
    public int hashCode() {
      int result = super.hashCode;
      if (result == 0) {
        result = unknownFields().hashCode();
        result = result * 37 + (link != null ? link.hashCode() : 0);
        super.hashCode = result;
      }
      return result;
    }

    @Override
    public String toString() {
      StringBuilder builder = new StringBuilder();
      if (link != null) builder.append(", link=").append(link);
      return builder.replace(0, 2, "Facebook{").append('}').toString();
    }

    public static final class Builder extends Message.Builder<Facebook, Builder> {
      public String link;

      public Builder() {
      }

      /**
       * / リンク
       */
      public Builder link(String link) {
        this.link = link;
        return this;
      }

      @Override
      public Facebook build() {
        return new Facebook(link, buildUnknownFields());
      }
    }

    private static final class ProtoAdapter_Facebook extends ProtoAdapter<Facebook> {
      ProtoAdapter_Facebook() {
        super(FieldEncoding.LENGTH_DELIMITED, Facebook.class);
      }

      @Override
      public int encodedSize(Facebook value) {
        return (value.link != null ? ProtoAdapter.STRING.encodedSizeWithTag(1, value.link) : 0)
            + value.unknownFields().size();
      }

      @Override
      public void encode(ProtoWriter writer, Facebook value) throws IOException {
        if (value.link != null) ProtoAdapter.STRING.encodeWithTag(writer, 1, value.link);
        writer.writeBytes(value.unknownFields());
      }

      @Override
      public Facebook decode(ProtoReader reader) throws IOException {
        Builder builder = new Builder();
        long token = reader.beginMessage();
        for (int tag; (tag = reader.nextTag()) != -1;) {
          switch (tag) {
            case 1: builder.link(ProtoAdapter.STRING.decode(reader)); break;
            default: {
              FieldEncoding fieldEncoding = reader.peekFieldEncoding();
              Object value = fieldEncoding.rawProtoAdapter().decode(reader);
              builder.addUnknownField(tag, fieldEncoding, value);
            }
          }
        }
        reader.endMessage(token);
        return builder.build();
      }

      @Override
      public Facebook redact(Facebook value) {
        Builder builder = value.newBuilder();
        builder.clearUnknownFields();
        return builder.build();
      }
    }
  }

  public static final class Google extends Message<Google, Google.Builder> {
    public static final ProtoAdapter<Google> ADAPTER = new ProtoAdapter_Google();

    private static final long serialVersionUID = 0L;

    public static final String DEFAULT_LINK = "";

    /**
     * / リンク
     */
    @WireField(
        tag = 1,
        adapter = "com.squareup.wire.ProtoAdapter#STRING"
    )
    public final String link;

    public Google(String link) {
      this(link, ByteString.EMPTY);
    }

    public Google(String link, ByteString unknownFields) {
      super(ADAPTER, unknownFields);
      this.link = link;
    }

    @Override
    public Builder newBuilder() {
      Builder builder = new Builder();
      builder.link = link;
      builder.addUnknownFields(unknownFields());
      return builder;
    }

    @Override
    public boolean equals(Object other) {
      if (other == this) return true;
      if (!(other instanceof Google)) return false;
      Google o = (Google) other;
      return Internal.equals(unknownFields(), o.unknownFields())
          && Internal.equals(link, o.link);
    }

    @Override
    public int hashCode() {
      int result = super.hashCode;
      if (result == 0) {
        result = unknownFields().hashCode();
        result = result * 37 + (link != null ? link.hashCode() : 0);
        super.hashCode = result;
      }
      return result;
    }

    @Override
    public String toString() {
      StringBuilder builder = new StringBuilder();
      if (link != null) builder.append(", link=").append(link);
      return builder.replace(0, 2, "Google{").append('}').toString();
    }

    public static final class Builder extends Message.Builder<Google, Builder> {
      public String link;

      public Builder() {
      }

      /**
       * / リンク
       */
      public Builder link(String link) {
        this.link = link;
        return this;
      }

      @Override
      public Google build() {
        return new Google(link, buildUnknownFields());
      }
    }

    private static final class ProtoAdapter_Google extends ProtoAdapter<Google> {
      ProtoAdapter_Google() {
        super(FieldEncoding.LENGTH_DELIMITED, Google.class);
      }

      @Override
      public int encodedSize(Google value) {
        return (value.link != null ? ProtoAdapter.STRING.encodedSizeWithTag(1, value.link) : 0)
            + value.unknownFields().size();
      }

      @Override
      public void encode(ProtoWriter writer, Google value) throws IOException {
        if (value.link != null) ProtoAdapter.STRING.encodeWithTag(writer, 1, value.link);
        writer.writeBytes(value.unknownFields());
      }

      @Override
      public Google decode(ProtoReader reader) throws IOException {
        Builder builder = new Builder();
        long token = reader.beginMessage();
        for (int tag; (tag = reader.nextTag()) != -1;) {
          switch (tag) {
            case 1: builder.link(ProtoAdapter.STRING.decode(reader)); break;
            default: {
              FieldEncoding fieldEncoding = reader.peekFieldEncoding();
              Object value = fieldEncoding.rawProtoAdapter().decode(reader);
              builder.addUnknownField(tag, fieldEncoding, value);
            }
          }
        }
        reader.endMessage(token);
        return builder.build();
      }

      @Override
      public Google redact(Google value) {
        Builder builder = value.newBuilder();
        builder.clearUnknownFields();
        return builder.build();
      }
    }
  }

  public static final class Line extends Message<Line, Line.Builder> {
    public static final ProtoAdapter<Line> ADAPTER = new ProtoAdapter_Line();

    private static final long serialVersionUID = 0L;

    public static final String DEFAULT_LINK = "";

    /**
     * / リンク
     */
    @WireField(
        tag = 1,
        adapter = "com.squareup.wire.ProtoAdapter#STRING"
    )
    public final String link;

    public Line(String link) {
      this(link, ByteString.EMPTY);
    }

    public Line(String link, ByteString unknownFields) {
      super(ADAPTER, unknownFields);
      this.link = link;
    }

    @Override
    public Builder newBuilder() {
      Builder builder = new Builder();
      builder.link = link;
      builder.addUnknownFields(unknownFields());
      return builder;
    }

    @Override
    public boolean equals(Object other) {
      if (other == this) return true;
      if (!(other instanceof Line)) return false;
      Line o = (Line) other;
      return Internal.equals(unknownFields(), o.unknownFields())
          && Internal.equals(link, o.link);
    }

    @Override
    public int hashCode() {
      int result = super.hashCode;
      if (result == 0) {
        result = unknownFields().hashCode();
        result = result * 37 + (link != null ? link.hashCode() : 0);
        super.hashCode = result;
      }
      return result;
    }

    @Override
    public String toString() {
      StringBuilder builder = new StringBuilder();
      if (link != null) builder.append(", link=").append(link);
      return builder.replace(0, 2, "Line{").append('}').toString();
    }

    public static final class Builder extends Message.Builder<Line, Builder> {
      public String link;

      public Builder() {
      }

      /**
       * / リンク
       */
      public Builder link(String link) {
        this.link = link;
        return this;
      }

      @Override
      public Line build() {
        return new Line(link, buildUnknownFields());
      }
    }

    private static final class ProtoAdapter_Line extends ProtoAdapter<Line> {
      ProtoAdapter_Line() {
        super(FieldEncoding.LENGTH_DELIMITED, Line.class);
      }

      @Override
      public int encodedSize(Line value) {
        return (value.link != null ? ProtoAdapter.STRING.encodedSizeWithTag(1, value.link) : 0)
            + value.unknownFields().size();
      }

      @Override
      public void encode(ProtoWriter writer, Line value) throws IOException {
        if (value.link != null) ProtoAdapter.STRING.encodeWithTag(writer, 1, value.link);
        writer.writeBytes(value.unknownFields());
      }

      @Override
      public Line decode(ProtoReader reader) throws IOException {
        Builder builder = new Builder();
        long token = reader.beginMessage();
        for (int tag; (tag = reader.nextTag()) != -1;) {
          switch (tag) {
            case 1: builder.link(ProtoAdapter.STRING.decode(reader)); break;
            default: {
              FieldEncoding fieldEncoding = reader.peekFieldEncoding();
              Object value = fieldEncoding.rawProtoAdapter().decode(reader);
              builder.addUnknownField(tag, fieldEncoding, value);
            }
          }
        }
        reader.endMessage(token);
        return builder.build();
      }

      @Override
      public Line redact(Line value) {
        Builder builder = value.newBuilder();
        builder.clearUnknownFields();
        return builder.build();
      }
    }
  }

  public static final class Others extends Message<Others, Others.Builder> {
    public static final ProtoAdapter<Others> ADAPTER = new ProtoAdapter_Others();

    private static final long serialVersionUID = 0L;

    public static final String DEFAULT_COPY = "";

    /**
     * / コピー用リンク
     */
    @WireField(
        tag = 1,
        adapter = "com.squareup.wire.ProtoAdapter#STRING"
    )
    public final String copy;

    public Others(String copy) {
      this(copy, ByteString.EMPTY);
    }

    public Others(String copy, ByteString unknownFields) {
      super(ADAPTER, unknownFields);
      this.copy = copy;
    }

    @Override
    public Builder newBuilder() {
      Builder builder = new Builder();
      builder.copy = copy;
      builder.addUnknownFields(unknownFields());
      return builder;
    }

    @Override
    public boolean equals(Object other) {
      if (other == this) return true;
      if (!(other instanceof Others)) return false;
      Others o = (Others) other;
      return Internal.equals(unknownFields(), o.unknownFields())
          && Internal.equals(copy, o.copy);
    }

    @Override
    public int hashCode() {
      int result = super.hashCode;
      if (result == 0) {
        result = unknownFields().hashCode();
        result = result * 37 + (copy != null ? copy.hashCode() : 0);
        super.hashCode = result;
      }
      return result;
    }

    @Override
    public String toString() {
      StringBuilder builder = new StringBuilder();
      if (copy != null) builder.append(", copy=").append(copy);
      return builder.replace(0, 2, "Others{").append('}').toString();
    }

    public static final class Builder extends Message.Builder<Others, Builder> {
      public String copy;

      public Builder() {
      }

      /**
       * / コピー用リンク
       */
      public Builder copy(String copy) {
        this.copy = copy;
        return this;
      }

      @Override
      public Others build() {
        return new Others(copy, buildUnknownFields());
      }
    }

    private static final class ProtoAdapter_Others extends ProtoAdapter<Others> {
      ProtoAdapter_Others() {
        super(FieldEncoding.LENGTH_DELIMITED, Others.class);
      }

      @Override
      public int encodedSize(Others value) {
        return (value.copy != null ? ProtoAdapter.STRING.encodedSizeWithTag(1, value.copy) : 0)
            + value.unknownFields().size();
      }

      @Override
      public void encode(ProtoWriter writer, Others value) throws IOException {
        if (value.copy != null) ProtoAdapter.STRING.encodeWithTag(writer, 1, value.copy);
        writer.writeBytes(value.unknownFields());
      }

      @Override
      public Others decode(ProtoReader reader) throws IOException {
        Builder builder = new Builder();
        long token = reader.beginMessage();
        for (int tag; (tag = reader.nextTag()) != -1;) {
          switch (tag) {
            case 1: builder.copy(ProtoAdapter.STRING.decode(reader)); break;
            default: {
              FieldEncoding fieldEncoding = reader.peekFieldEncoding();
              Object value = fieldEncoding.rawProtoAdapter().decode(reader);
              builder.addUnknownField(tag, fieldEncoding, value);
            }
          }
        }
        reader.endMessage(token);
        return builder.build();
      }

      @Override
      public Others redact(Others value) {
        Builder builder = value.newBuilder();
        builder.clearUnknownFields();
        return builder.build();
      }
    }
  }

  private static final class ProtoAdapter_SocialShares extends ProtoAdapter<SocialShares> {
    ProtoAdapter_SocialShares() {
      super(FieldEncoding.LENGTH_DELIMITED, SocialShares.class);
    }

    @Override
    public int encodedSize(SocialShares value) {
      return (value.twitter != null ? Twitter.ADAPTER.encodedSizeWithTag(1, value.twitter) : 0)
          + (value.facebook != null ? Facebook.ADAPTER.encodedSizeWithTag(2, value.facebook) : 0)
          + (value.google != null ? Google.ADAPTER.encodedSizeWithTag(3, value.google) : 0)
          + (value.line != null ? Line.ADAPTER.encodedSizeWithTag(4, value.line) : 0)
          + (value.others != null ? Others.ADAPTER.encodedSizeWithTag(5, value.others) : 0)
          + value.unknownFields().size();
    }

    @Override
    public void encode(ProtoWriter writer, SocialShares value) throws IOException {
      if (value.twitter != null) Twitter.ADAPTER.encodeWithTag(writer, 1, value.twitter);
      if (value.facebook != null) Facebook.ADAPTER.encodeWithTag(writer, 2, value.facebook);
      if (value.google != null) Google.ADAPTER.encodeWithTag(writer, 3, value.google);
      if (value.line != null) Line.ADAPTER.encodeWithTag(writer, 4, value.line);
      if (value.others != null) Others.ADAPTER.encodeWithTag(writer, 5, value.others);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public SocialShares decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.twitter(Twitter.ADAPTER.decode(reader)); break;
          case 2: builder.facebook(Facebook.ADAPTER.decode(reader)); break;
          case 3: builder.google(Google.ADAPTER.decode(reader)); break;
          case 4: builder.line(Line.ADAPTER.decode(reader)); break;
          case 5: builder.others(Others.ADAPTER.decode(reader)); break;
          default: {
            FieldEncoding fieldEncoding = reader.peekFieldEncoding();
            Object value = fieldEncoding.rawProtoAdapter().decode(reader);
            builder.addUnknownField(tag, fieldEncoding, value);
          }
        }
      }
      reader.endMessage(token);
      return builder.build();
    }

    @Override
    public SocialShares redact(SocialShares value) {
      Builder builder = value.newBuilder();
      if (builder.twitter != null) builder.twitter = Twitter.ADAPTER.redact(builder.twitter);
      if (builder.facebook != null) builder.facebook = Facebook.ADAPTER.redact(builder.facebook);
      if (builder.google != null) builder.google = Google.ADAPTER.redact(builder.google);
      if (builder.line != null) builder.line = Line.ADAPTER.redact(builder.line);
      if (builder.others != null) builder.others = Others.ADAPTER.redact(builder.others);
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
